# Data Analysis Report

## Source: confluence
**Title**: Runbook: Deploy / Upgrade / Roll Back perf-canary (Prod)
**Content length**: 12894 characters

**Preview (first 1000 chars)**:
```text
## Purpose
This runbook describes the operational procedures to deploy, upgrade, roll back, and safely disable the **perf-canary** service across regions.

perf-canary is a lightweight, always-on synthetic workload that calls internal inference endpoints for a curated model set and emits performance metrics (p50/p95/p99, tokens/sec, prefill/decode, queueing time, batch stats). The service must stay under the defined overhead budget and must not capture or emit customer data.

**Primary users:** Eng-Infra on-call and Release Engineering during rollouts.

---

## Quick reference
**Service name:** perf-canary

**Deployment mechanism:**
- Terraform: service account / IAM / secrets / region wiring
- Helm: workload deployment and per-region config

**Primary dashboards:**
- Grafana: "Perf Canary – Per Model" (p95, tokens/sec, breakdowns)
- Capacity/overhead: GPU utilization + canary QPS counters

**Kill switch:** control-plane config (disable flag) + Helm scale-to-zero (last resort)

**Slack
```

---

## Source: fireflies
**Title**: Tethys Systems - SI Channel & Cloud GPU Fit Discovery
**Content length**: 9980 characters

**Preview (first 1000 chars)**:
```text
summary:
Introductory discovery between Redwood and Tethys Systems to evaluate a channel+SI partnership. Discussed integration options (hosted API vs. dedicated vs. private), expected customer use-cases (real-time chat, high-throughput batch embeddings), Tethys go-to-market model (reseller + managed services), rough commercial assumptions, and an initial pilot framework. Action items: share partner packet, provide VPC/private deploy checklist, run a short perf baseline with Tethys sample workload, and schedule executive alignment next week.

transcript:
Meeting Header:
Date: 2025-03-27
Time: 15:00 UTC
Duration: 62 minutes
Attendees: Maya Patel (Redwood AE); Jonas Reed (Redwood SE); Sofia Alvarez (Tethys); Ravi Menon (Tethys); Diane Cho (Tethys)

Optional autogenerated sections:
- Topics: partner GTM, technical fit, pilot scope
- Action items: listed below

Transcript:
[00:00] Maya Patel: Hey everyone, thanks for joining. I'm Maya, AE on the Redwood partnerships team. Uh, quick round of
```

---

## Source: github
**Title**: session grace-period and adaptive retry-weighting across Py/TS/Go SDKs
**Content length**: 2429 characters

**Preview (first 1000 chars)**:
```text
description:
Motivation: address two customer-visible mismatches across SDKs: 1) session token grace/TTL handling caused transient 401s during small clock skew and rapid handoffs, and 2) inconsistent retry weighting for streaming vs request/response calls resulted in uneven load on connector pools. This PR implements a lightweight session-grace window, normalizes TTL refresh semantics, and introduces an adaptive retry-weight multiplier for streaming consumers so retries do not hammer the connector during backoff storms. Changes span Python, TypeScript, and Go SDKs and include tests, examples, and a minor version bump for each SDK. Checklist: - [x] implement session grace window (py/ts/go) - [x] unify TTL refresh path and prioritize background refresh - [x] add adaptive retry-weight multiplier (configurable) - [x] add integration test harness for race scenario - [x] update README and changelogs - [x] bump package versions (py:1.18.0, ts:2.7.1, go:1.5.3) Related work: Linear ENG-4129, EN
```

---

## Source: gmail
**Title**: Escalation: Rollback guarantees + upgrade support coverage for Redwood Private (Cascade Financial Group)
**Content length**: 11105 characters

**Preview (first 1000 chars)**:
```text
From: Alyssa Chen <alyssa.chen@cascadefg.com>
To: Markus Klein <markus.klein@redwoodinference.com>
Cc: Tom Becker <tom.becker@cascadefg.com>, Rachel Kim <rachel.kim@redwoodinference.com>
Date: Tue, Jun 3, 2025 at 9:12 AM
Subject: Escalation: rollback guarantees + upgrade support coverage (Redwood Private)

Markus,

We’re preparing our next Redwood Private upgrade in our VPC environment and need written clarity on two items before we’ll schedule the maintenance window:

1) Rollback guarantees: if the upgrade fails (including DB migration issues), can we roll back to the prior version and what is the expected time to restore service? We need something more concrete than “best effort.”

2) Support coverage: we will be doing this in our standard maintenance window (Saturday 1:00–4:00am ET). We need to know if Redwood will have engineering/SRE coverage during the window, and what escalation path exists if rollback is required.

We’ve had issues with other vendors where “rollback” meant “reb
```

---

## Source: google_drive
**Title**: routing-and-controlplane-runbook-architecture-notes
**Content length**: 9716 characters

**Preview (first 1000 chars)**:
```text
Purpose
This doc is a combined architecture + operational runbook for our routing policy engine and control-plane components across multi-region deployments. It is intentionally practical: short diagnostics, commands, and mitigations embedded in architecture context so on-call engineers can both reason about root cause and act quickly.

Scope
- Components: routing-policy-service (RPS), control-plane-api, policy-store (etcd/consul), route-agent (edge sidecar), regional health aggregator, and the global policy router.
- Cross-region concerns: affinity, KV replication lag, split-brain on leader election, and fallback model variant routing.
- Not included: serving-runtime GPU node-level debugging (see serving-runtime/sre-runbooks).

Architecture notes (brief)
- Control plane (control-plane-api + policy-store) is the single source of truth for routing policies, rollout stages, and region weightings. It exposes a versioned API used by route-agents to fetch policies on startup and at policy-c
```

---

## Source: hubspot
**Title**: Bayberry HostedAI Solutions
**Content length**: 2718 characters

**Preview (first 1000 chars)**:
```text
use_case_summary:
Customer support SaaS that will embed an assistant in-app (chat) and run background reranking + embeddings for search. Primary goals: fast response for conversational flows (p50 < 350ms), reasonable token cost for heavy daily active users, and predictable monthly spend. Want hosted API for speed of integration and self-serve before committing to Dedicated.

notes:
Lead profile: product-led SMB, mature engineering (React + Node), 2 ML engineers. Quote from CTO: 'We need something that beats our current open-source stack on latency but doesn't blow the budget.' Key theme: model-selection questions. Specific asks: - How do Llama 2/3 variants compare to Mistral/Qwen for short conversational latency vs output quality? - Any recommended cost-effective flavors (quantized or smaller context length) for high-concurrency chat? - Can we route cheap requests to smaller models and fall back to higher-quality for escalation? - Any streaming token/pricing caveats we should budget fo
```

---

## Source: jira
**Title**: High P95 tail latency after enabling new quantization profile + KV prefetch — request assisted safe-toggle checklist
**Content length**: 8153 characters

**Preview (first 1000 chars)**:
```text
description:
Issue summary:
Customer deployed two optimizer changes in prod earlier today: a new quantization profile (profile id qp-2026-03-13-v3) and enabled kv_prefetch=true at the serving-group level. Within ~30 minutes they observed a sharp increase in P95 token latency from ~120ms to ~620ms and intermittent request timeouts for long streaming requests. The issue impacts multiple routes and is causing user-facing latency spikes for LexiHealth's triage assistant flows.

Impact:
- Increased P95 token latency across affected model variants.
- Streaming sessions hitting websocket disconnects after 10–20s in ~5% of requests.
- Business impact: slowdown of patient triage flows; customer marked as Priority-1.

Environment:
- Redwood Dedicated pool: rwd-dedicated-lexi-01 (prod us-east)
- Serving groups: sg-lexi-triage-a, sg-lexi-triage-b
- Model variants: redwood-open-7b-quant-8bit (pinned), gptx-13b-qat (fallback)

Recent changes (per customer):
1) Applied quant profile: qp-2026-03-13-v3
```

---

## Source: linear
**Title**: Compact interactive example cards and annotated diff overlays for embeddings + rerank quickstart
**Content length**: 2812 characters

**Preview (first 1000 chars)**:
```text
description:
Design and produce a compact set of interactive example cards and annotated diff overlays that will be used in the Embeddings & Rerank quickstart docs. The goal is a digestible, mobile-friendly visual module that demonstrates: 1) building an embedding index, 2) running a naive similarity retrieval, 3) applying rerank to reorder results, and 4) visualizing the rerank score delta (before/after) with callouts for why items moved. Must be small enough for hero placement on the quickstart page and also export as separate assets for the docs and Console walkthrough.

tasks:
Design 4 compact card templates: Query, Raw Hits (unranked), Rerank Hits (post-rerank), Score Diff overlay
Create annotated overlay system that highlights token-level reasons (e.g., 'topic match', 'entity overlap', 'context window')
Add interactive slider prototype to simulate rerank weight from 0.0 -> 1.0 (Figma prototype)
Produce responsive exports: 1200x600 hero PNG, 600x320 card PNG, SVG for docs with acc
```

---

## Source: slack
**Title**: eng-runtime
**Content length**: 6319 characters

**Preview (first 1000 chars)**:
```text
sasha: Heads up — we started seeing a 2.5-3x increase in p95/p99 latency on prod-us-west LLM endpoints around the time of the OS/driver rollout (driver 535.54 -> 536.12). p50 largely unchanged. Repro is high and correlated with a recent Triton build we deployed last week.
kevin: Thanks — any noisy neighbor alerts? GPU memory pressure?
sasha: No noisy neighbor alarms from SRE, but metric spikes align with larger KV cache evictions and extra host<->device copies. Throughput dropped ~30% on the 8-GPU pool.
maria: Can you paste the perf profile (nvprof / Nsight) or the runtime kernel selection logs?
sasha: Pushing logs. Key snippets:
```
[server] selected_kernel: flash_attn_fwd_v2 not available for model: redwood-7b, sequence_len=1024, dtype=f16
[server] fallback_kernel: using attention_generic_tiled
[triton] kernel_launch_time_ms: attention_generic_tiled 3.8ms (prev: 0.9ms using flash_attn_fwd_v2)
```
kevin: That shows exactly it — Triton/FlashAttention detection isn't picking the optimiz
```

---

