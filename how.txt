# 1. Vào tmux để chạy ngầm
tmux

# 2. Khởi tạo lệnh conda cho Terminal
source /network-volume/miniconda3/bin/activate

# 3. Tạo môi trường từ file environment.yml (chỉ chạy 1 lần)
conda env create -f environment.yml

# 4. Kích hoạt môi trường vừa tạo (tên môi trường là trag)
conda activate trag

# 5. Chạy benchmark (quá trình này lâu nên bạn có thể tắt máy đi ngủ)
python src/run_benchmark.py --limit 10


=====

tmux attach

python src/run_benchmark.py --limit 10

=====

conda activate trag

nohup ./run_all.sh > benchmark_logs.txt 2>&1 &

tail -f benchmark_logs.txt

or:

./run_all_smoke_tests.sh > benchmark_smoke_logs.txt 2>&1 &

tail -f benchmark_smoke_logs.txt

pkill -f "run_all" ; pkill -f "run_benchmark.py" ; pkill -f "vllm" ; pkill -f "run_bm25" ; pkill -f "run_vector" ; sleep 2 ; ps aux | grep python