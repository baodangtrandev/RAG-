# s44443-025-00311-z

## Page 1

Journal of King Saud University Computer and Information Sciences (2025) 37:267
https://doi.org/10.1007/s44443-025-00311-z
ORIGINAL PAPER
LLM-augmented multi-agent cooperative framework for medical case
retrieval in cardiology
Lang Deng 1,2 · Huanhuan Hu 1,2 · Kongjie Lu 1,2 · Ping He 1,2
Received: 19 August 2025 / Accepted: 22 September 2025 / Published online: 20 October 2025
© The Author(s) 2025
Abstract
Retrieving relevant medical cases or documents is a critical information retrieval (IR) task in clinical decision support,
particularly in cardiology, yet traditional search methods struggle with complex semantic queries in healthcare. Recent
advances in large language models offer powerful language understanding, but LLMs alone cannot reliably retrieve factual
cases due to knowledge cutoffs and hallucinations. We focus on a case retrieval task – given a textual description of a patient
case, ﬁnd similar prior cases or pertinent literature – formulated as a general IR problem rather than a purely medical study.
Conventional lexical methods often miss semantic similarities, while static dense retrievers falter on out-of-domain medical
vocabularies. LLMs can comprehend queries and context, but without external knowledge they may produce inaccurate or
non-transparent results. We propose a novel LLM-augmented multi-agent retrieval framework that marries an LLM with
dedicated retrieval agents in an iterative cooperation mechanism. Our method uses a LLM as a “planner” agent to reformulate
queries and integrate medical (e.g., cardiology) context, and a retrieval agent (with a knowledge index) to fetch candidate
cases; the agents interact iteratively, reﬁning search and reranking results via a retrieval-augmented generation (RAG) loop.
This multi-agent design contributes three innovations: (1) an iterative query reﬁnement strategy guided by LLM reasoning
chains; (2) a cooperative retrieval architecture where an LLM agent and a search agent exchange information to improve
relevance; (3) an LLM-based relevance estimator that grounds the LLM with retrieved evidence to mitigate hallucinations.
Experiments on three open medical text datasets show our method outperforms baseline models by 5.3–6.1 percentage points
in Recall@10 and NDCG, with statistically signiﬁcant gains. We also observe improved generalization to novel conditions
and robustness to query noise compared to baselines. The proposed framework, while validated on medical text, is broadly
applicable to other knowledge-intensive retrieval tasks (legal case search, technical support archives), providing a foundation
for intelligent IR systems that leverage both learning-based understanding and explicit retrieval for transparency and up-to-date
knowledge.
Keywords Natural language processing · Multi-agent retrieval · Large language models · Retrieval-augmented generation ·
Medical information retrieval
1 Introduction
Retrieving relevant textual information is a fundamental
problem in computer science, with applications ranging
from web search to recommender systems. In specialized
Lang Deng and Huanhuan Hu contributed equally to this work and were
considered as co-ﬁrst authors.
B Kongjie Lu
B Ping He
hepink523@163.com
1 Huzhou Central Hospital, Fifth School of Clinical Medicine of
Zhejiang Chinese Medical University, Huzhou 313000, China
2 Huzhou Central Hospital, Afﬁliated Central Hospital of
Huzhou University, Huzhou 313000, China
domains like healthcare, medical case retrieval – ﬁnding
similar patient cases or pertinent clinical literature given a
query case – is crucial for evidence-based medicine (Tsatsa-
ronis et al. 2015) and clinical decision support (Roberts et al.
2020). This task can be cast as an information retrieval (IR)
problem over unstructured text (electronic health records,
case reports, Q&A logs, etc.), and it poses unique challenges
to conventional IR techniques. Traditional keyword-based
methods (e.g. BM25) often fail to capture the semantic and
contextual richness of clinical language (Robertson et al.
2009). For example, a query describing “chest pain and
diaphoresis in a 55-year-old” might not explicitly match doc-
uments containing “myocardial infarction symptoms,” yet a
human expert recognizes them as related – a gap that simple
123

## Page 2

267 Page 2 of 19 Journal of King Saud University Computer and Information Sciences (2025) 37 :267
lexical matching misses. Neural IR models have improved
semantic matching by encoding text into vector representa-
tions (Karpukhin et al. 2020; Reimers and Gurevych 2019).
BERT-based retrievers and re-rankers can capture contex-
tual similarity beyond exact words (Nogueira and Cho 2019;
Khattab and Zaharia 2020). However, these models typically
require large training data with relevance labels and strug-
gle when applied to medical text without domain adaptation.
Domain-speciﬁc language models like BioBERT and Clin-
icalBERT (Lee et al. 2020; Alsentzer et al. 2019) partially
address medical terminology, but they may not generalize
well to rare conditions or new medical ﬁndings, leading to
suboptimal recall in retrieval tasks on medical data (Ham-
barde and Proenca 2023).
Meanwhile, large language models (LLMs) such as GPT-
3 and GPT-4 have demonstrated remarkable capabilities in
understanding and generating human-like text (Brown et al.
2020; Bubeck et al. 2023). Notably, LLMs are few-shot
learners that can perform complex tasks with minimal task-
speciﬁc training (Brown et al. 2020). In the medical domain,
LLMs have shown the ability to encode a surprising amount
of clinical knowledge (Singhal et al. 2023) and answer medi-
cal questions at an expert level in certain evaluations (Singhal
et al. 2023; Wang et al. 2024). This suggests they grasp
domain semantics well. However, using LLMs alone for
retrieval is problematic. LLMs are generative and lack direct
access to up-to-date external knowledge, relying only on their
training data (Ji et al. 2023). They often produce halluci-
nated content – plausible-sounding but incorrect information
(Bubeck et al. 2023) – which is unacceptable when query-
ing for evidence in healthcare. Additionally, LLMs do not
inherently return sources or document identiﬁers for their
answers, limiting transparency. Simply asking an LLM to
recall similar cases is akin to a “closed-book” approach and
will miss relevant cases not seen during training. Therefore,
LLMs cannot replace explicit retrieval engines when accu-
rate and veriﬁable case lookup is required (Ji et al. 2023;
Lewis et al. 2020).
A promising direction is to integrate LLMs with retrieval
– harnessing the comprehension and reasoning abilities of
LLMs together with the grounding of IR systems. Retrieval-
Augmented Generation (RAG) techniques have emerged to
incorporate external documents into LLM outputs (Lewis
et al. 2020; Gao et al. 2023). By fetching relevant text
snippets and feeding them into the LLM’s context, RAG
can improve factual accuracy and reduce hallucinations in
domains like healthcare (Gao et al. 2023; Zhao et al. 2024).
For instance, Lewis et al. ( 2020) demonstrate that query-
ing a knowledge base for supporting evidence signiﬁcantly
boosts performance on knowledge-intensive tasks. However,
most existing RAG frameworks treat retrieval as a single-
step tool call: the LLM issues one query to a search module,
gets some documents, and then generates an answer (Lewis
et al. 2020; Y ao et al. 2023). This one-shot approach may
be insufﬁcient for complex case queries that require itera-
tive exploration. In medical case retrieval, a query might be
initially broad or underspeciﬁed – e.g., “atypical chest pain
case” – requiring follow-up clariﬁcation (Which features are
most relevant? What speciﬁc context?). Humans often con-
duct multi-turn searches, reformulating queries after seeing
initial results. Recent work on LLM-based agents suggests
that LLMs can emulate such interactive query reformula-
tion. Y ao et al. ( 2023) introduced ReAct, where the LLM
interleaves reasoning and tool use, enabling multi-step query-
answering. Similarly, Press et al. (He et al. 2025) and others
propose letting the model ask itself follow-up questions (self-
asking) and reﬁne queries iteratively. These approaches hint
that an LLM could guide a search process through multiple
turns, acting as a high-level decision-maker or “agent” that
decides when and how to query the data.
In parallel, the concept of multi-agent systems with LLMs
is gaining traction (Shen et al. 2023; Nachimovsky et al.
2025). Instead of a single monolithic model, multiple spe-
cialized agents (which could be instances of LLMs or a
mix of models) can collaborate on sub-tasks, communi-
cating in natural language. For example, one agent might
specialize in planning and another in execution (Shen et al.
2023). In IR, a recent perspective by Nachimovsky et al.
(2025) advocates modeling retrieval as interactions between
query agents, document agents, and ranker agents. This is
particularly relevant in dynamic environments where both
queries and documents might be generated or analyzed by
AI. A multi-agent viewpoint allows us to break the complex
retrieval problem into modular components: one agent for-
mulates reﬁned queries, another fetches documents, another
evaluates them, etc., all coordinated towards a common goal.
Such a framework can potentially capture reasoning loops
and negotiation that a single-step method cannot (Nachi-
movsky et al. 2025). While multi-agent IR is still nascent,
early explorations show improved performance when agents
handle different aspects of the search process (Nachimovsky
et al. 2025).
Considering these developments, we identify a critical
research gap: existing IR methods (both classical and neu-
ral) struggle with the semantic and generalization demands
of medical case retrieval, and naive LLM integration (single-
step RAG) does not fully leverage the LLM’s reasoning
potential. There is a need for an innovative computing method
that brings together the strengths of LLMs and IR in a more
synergistic, interactive manner. Our work addresses this by
introducing a cooperative multi-agent retrieval framework
centered on LLM guidance, with the medical case domain as
123

## Page 3

Journal of King Saud University Computer and Information Sciences (2025) 37 :267 Page 3 of 19 267
a challenging application to validate its effectiveness. Impor-
tantly, our focus is on methodological innovation in IR and
NLP (the medical aspect is a testbed, not the primary contri-
bution).
We explicitly highlight the key methodological contribu-
tions of our study:
1. We propose a novel retrieval framework where a planner
agent (powered by an LLM) and a search agent (IR mod-
ule) collaborate in multiple rounds to retrieve relevant
cases. Unlike one-shot RAG, our architecture enables
iterative querying and dynamic feedback.
2. We develop a technique for iterative query reﬁnement
using LLM reasoning. The LLM agent generates clarify-
ing questions and reformulated queries based on initial
results, akin to a human search strategist. We incorpo-
rate a chain-of-thought prompting scheme in the LLM to
allow it to explain the query logic and determine what to
search next. This leads to more effective searches, espe-
cially for complex medical queries with multiple facets.
3. At the core of our retrieval agent, we employ a hybrid
model that combines dense semantic retrieval with lexical
matching. We ﬁne-tune a bi-encoder model on medical
text for encoding cases, ensuring domain-speciﬁc termi-
nology is understood. Additionally, we integrate the LLM
agent as a reranker that evaluates retrieved candidates in
natural language. By fusing model-driven relevance scor-
ing with LLM-based judgment, our method achieves high
recall and precision, addressing the domain adaptation
issue in prior work.
4. We conduct comprehensive experiments on three bench-
mark datasets covering clinical narratives and biomedical
literature. We compare against a broad spectrum of 10+
baselines, results show consistent improvements of 4–8%
absolute on ranking metrics (NDCG, Recall), demon-
strating the efﬁcacy of our approach. We also perform
ablation studies to quantify the contribution of each com-
ponent, cross-domain generalization tests, and robustness
checks. Furthermore, we analyze case studies to illustrate
how the LLM agent’s explanations enhance interpretabil-
ity, a crucial aspect for clinical adoption.
Overall, our work advances the state-of-the-art in infor-
mation retrieval by introducing a cooperative LLM-agent
paradigm that is particularly beneﬁcial for knowledge-
intensive domains. While validated on medical case retrieval,
the proposed techniques are general and lay the groundwork
for applying multi-agent LLM-augmented retrieval in other
domains (legal case retrieval, technical support archives,
etc.), aligning with the broader push towards explainable and
generalizable AI systems in IR.
2 Related work
2.1 Neural information retrieval and case-based
search
Traditional vs Neural IR Early IR methods relied on lex-
ical matching and probabilistic models. BM25 remains a
strong classical baseline, scoring documents by term fre-
quency and inverse document frequency (Robertson et al.
2009). However, BM25 cannot capture semantic similarity
beyond exact token overlap. The advent of deep learning
brought neural IR models that encode text in vector spaces
to measure semantic relevance (Reimers and Gurevych2019;
Mitra et al. 2018). For instance, the Deep Relevance Match-
ing Model (DRMM) introduced by Guo et al. (2016) applied
neural networks to model term-level interactions for ad-hoc
retrieval. More recent approaches learn end-to-end text repre-
sentations: DPR (Dense Passage Retrieval) uses twin BERT
encoders for questions and passages, trained on a contrastive
loss to retrieve Wikipedia passages for QA (Karpukhin et al.
2020). ColBERT reﬁnes this with late interaction, comput-
ing ﬁne-grained term embeddings and achieving efﬁcient
yet accurate passage search (Khattab and Zaharia 2020)’.
Extensive benchmarks like MS MARCO and BEIR have
driven progress in neural IR, revealing that dense retriev-
ers often outperform sparse methods on semantic matching
tasks (Thakur et al. 2021). In the biomedical domain, neural
IR has been applied to literature search and patient record
retrieval. For example, BioBERT-based retrievers leverage
domain-pretrained BERT to encode medical texts (Lee et al.
2020), improving over generic BERT on biomedical arti-
cles. Despite their success, neural retrievers face challenges:
they require large training sets of relevant query-document
pairs, and performance can degrade when queries are from
a different distribution than training data (the domain shift
problem). In medical IR, labeled data is scarce and expensive
to obtain, as relevance judgments need expert domain knowl-
edge. Some unsupervised or zero-shot techniques have been
explored, such as Contriever which uses contrastive learning
on unlabeled text to build general-purpose dense retrievers
(Gao et al. 2023). Gao et al. ( 2023) show that with careful
pre-training (e.g. inverse cloze tasks and corpus-level MLM),
zero-shot dense retrieval improves, but there remains a gap
when tackling highly specialized queries.
Case-based medical retrieval Retrieving similar patient
cases or relevant medical literature has been an area of inter-
est in biomedical informatics for decades. Early systems
used structured data or simple text ﬁlters to ﬁnd matching
records. The Text Retrieval Conference held a Clinical Deci-
sion Support (CDS) Track (2014–2016) speciﬁcally targeting
case-based retrieval: given a medical case narrative, the task
was to retrieve relevant journal articles for answering clin-
123

## Page 4

267 Page 4 of 19 Journal of King Saud University Computer and Information Sciences (2025) 37 :267
ical questions (diagnosis, test, treatment) (Tsatsaronis et al.
2015). Participants applied variations of BM25, knowledge-
driven query expansion, and later, initial neural rerankers as
BERT emerged. Simpson et al. (Kirk et al.2016) and Roberts
et al. (Kirk et al.2016; Roberts et al.2020) provide overviews
of these efforts. Results indicated that lexical methods with
domain-speciﬁc expansions (e.g., adding MeSH terms) were
effective baselines, but complex narratives remained chal-
lenging for automated systems. More recently, researchers
have begun applying modern neural IR to clinical data. For
example, dense embedding models have been used to identify
similar patients in electronic health records for cohort study
support (Miotto et al. 2016). Miotto et al. ( 2016) learned
unsupervised patient representations (“Deep Patient”) from
EHRs, which could be used to measure patient similar-
ity, though their focus was predictive modeling rather than
retrieval of records. Other works use deep metric learn-
ing to explicitly learn a patient similarity function: for
instance, convolutional neural networks over structured and
unstructured data to cluster similar patients (Thakur et al.
2021; Devlin et al. 2019) (ref. patient similarity networks).
However, these often treat similarity as a supervised clas-
siﬁcation/regression (predict some outcome or match), as
opposed to actual retrieval ranking. Our work differs by
directly tackling unstructured textual case retrieval with a
focus on generalization and using minimal supervised data.
We also emphasize interpretability; whereas a metric learn-
ing model might give a similarity score, our LLM agent can
provide natural-language rationales (e.g., “both patients have
a history of smoking and present with early-onset emphy-
sema”), aligning with the need for explainability in clinical
tools (Achiam et al. 2023; Touvron et al. 2023; Zakka et al.
2024).
2.2 Large language models and retrieval
augmentation
LLMs in knowledge-intensive tasks Large language models
like GPT-3 (Brown et al. 2020) and GPT-4 (Bubeck et al.
2023) have transformed NLP by demonstrating strong perfor-
mance in question answering, summarization, and dialogue
through few-shot prompts. However, for tasks requiring
up-to-date or detailed knowledge (e.g. “What is the lat-
est guideline for treating condition X?”), LLMs struggle
because their knowledge is frozen at training time and may
be incomplete or outdated (Ji et al. 2023). Researchers have
thus explored retrieval-augmentation: combining LLMs with
IR to ground their responses. Lewis et al. ( 2020)i n t r o -
duced Retrieval-Augmented Generation (RAG), where an
LLM-based generator is paired with a neural retriever to
incorporate external documents into answer generation. RAG
and related frameworks like REALM and FiD (Fusion-in-
Decoder) have shown signiﬁcant gains in open-domain QA,
fact-checking, and knowledge-intensive dialog by mitigat-
ing hallucinations and improving factual correctness (Lewis
et al. 2020; Gao et al. 2023; Jeong et al. 2024). In the med-
ical domain, Singhal et al. ( 2023) presented Med-PaLM (an
adaptation of GPT-4) and found that providing retrieval capa-
bility could help the model handle questions it otherwise
answered incorrectly. A recent survey by Gao et al. ( 2023)
systematically reviews RAG techniques for LLMs, classi-
fying approaches into naïve RAG (single-pass retrieve and
read) versus more advanced iterative retrieval and modular
generation. For instance, one study proposes iterative RAG
for medicine, where an LLM can ask follow-up queries based
on initial results (Zhao et al. 2024) – this resonates with our
approach of multi-turn retrieval. Another survey by Zhao
et al. ( 2024) discusses RAG for ensuring the provenance of
AI-generated content, reinforcing that retrieval helps in gen-
erating veriﬁable and trustworthy outputs. Our work builds
on these insights, extending the paradigm by using the LLM
not just as a passive consumer of retrieved facts, but as an
active controller that drives the retrieval process itself (Pal
et al. 2023; Xiong et al. 2024; Schick et al. 2023; Wei et al.
2022).
Interactive & multi-step retrieval with LLMs The idea of
LLMs actively querying and interacting with tools has been
explored in several contexts. The ReAct framework (Y ao
et al. 2023) demonstrated that an LLM (prompted suitably)
can produce a sequence of reasoning statements and tool-
use actions, enabling it to perform multi-step searches or
API calls. For example, given a complex question, a ReAct
agent might issue a search query, read the results, then rea-
son and search again, emulating human research behavior
(Y ao et al.2023). Similarly, Self-Ask prompting lets an LLM
break a question into sub-questions and answer them one
by one (possibly using a search engine for each) (He et al.
2025). These approaches showed improved performance on
multi-hop QA and logical reasoning tasks by decompos-
ing problems. In our scenario, a medical case query might
implicitly be multi-hop (symptoms - > possible conditions
-> similar case reports); an LLM that can iteratively search
should handle this better than one-shot retrieval. Beyond
single-agent prompting, recent systems use multiple LLMs
or modules in a pipeline or loop. Shen et al. ( 2023) intro-
duced HuggingGPT, where ChatGPT acts as a coordinator
that delegates tasks (e.g., image analysis, speech recognition)
to appropriate expert models. While their focus was multi-
modal AI tasks, the concept of an LLM as a central planner is
analogous to our planner agent. There is also growing inter-
est in multi-agent communication: could two or more LLM
agents converse to solve a problem? For instance, one agent
could play the role of a “user” or “questioner” asking clariﬁ-
cations, and another the “expert” answering, thereby reﬁning
123

## Page 5

Journal of King Saud University Computer and Information Sciences (2025) 37 :267 Page 5 of 19 267
the query through dialogue. This approach was explored in
dialog systems and brainstorming sessions between agents,
showing potential to improve quality via iterative feedback.
Park et al. and others have even simulated communities of
agents to collaborate on tasks (e.g., Generative Agents) –
albeit not for retrieval tasks (Nachimovsky et al. 2025). Our
framework can be seen as a speciﬁc instantiation of this trend:
we effectively create an agent that asks implicit questions (via
query reformulation) and another that provides answers (via
retrieval results), iterating until satisﬁed (Mialon et al. 2023;
Lazaridou et al. 2022; Chen et al. 2017;Q ue ta l .2020; Zhao
et al. 2023).
Domain adaptation & generalization A notable advantage
of integrating retrieval with LLMs is improved domain adapt-
ability. Pure LLM solutions struggle with domain-speciﬁc
jargon (e.g., medical abbreviations) unless ﬁne-tuned exten-
sively on that domain (Alsentzer et al. 2019;G ue ta l .
2021). In contrast, retrieval can bring in actual domain text
as needed. Domain-speciﬁc models like PubMedBERT (Gu
et al. 2021) and ClinicalBERT (Alsentzer et al. 2019)w e r e
developed to address this by pretraining on biomedical cor-
pora. They have been used as backbones for many medical
NLP tasks and typically outperform generic models on in-
domain evaluations. We leverage such models in our retrieval
backbone to initialize semantic embeddings. However, even
these models may not encode clinical reasoning out of the box
(they learn language patterns, not how to connect a patient
case to similar cases). There have been efforts to inject struc-
tured medical knowledge or train models to cite evidence
(Jeong et al. 2024). For example, Jeong et al. (2024) combine
retrieval with a self-reﬂection step for medical QA, show-
ing that retrieving relevant facts and then letting the model
reason yields better medical reasoning accuracy. Another
work by Nachimovsky et al. ( 2025) used retrieval to help an
LLM explain medical answers in lay language, essentially
augmenting domain knowledge. These indicate that hybrid
approaches can achieve both accuracy and explainability,
which purely neural end-to-end models lack. Our approach
further emphasizes explainability: by having an LLM agent
articulate why it is reﬁning a query or why a retrieved case is
relevant, we produce a trace of reasoning. This is important
for user trust, especially in medicine where clinicians must
understand the basis of recommendations. Prior IR systems
for healthcare rarely provided such reasoning, other than sim-
ple keyword highlighting. In contrast, our multi-agent system
can generate a summary of how a past case is analogous to
the query case, offering a form of case-based explanation
(Formal et al. 2021; Santhanam et al. 2021; Nogueira et al.
2019; Dai et al. 2022).
In summary, our work is grounded in several strands of
recent research: neural IR for semantic matching, LLM inte-
gration for knowledge augmentation, interactive multi-step
retrieval, and domain adaptation in biomedical NLP . By syn-
thesizing these directions, we aim to overcome their respec-
tive limitations—such as the rigidity of traditional IR, the
isolation of LLMs from external data, and the brittleness of
single-step pipelines. While prior work has explored individ-
ual aspects like ReAct-style reasoning, self-ask prompting,
multi-agent retrieval frameworks, and LLM-based reranking,
our method is among the ﬁrst to integrate these components
into a uniﬁed, cooperative multi-agent retrieval framework.
We further validate this approach systematically on chal-
lenging medical case retrieval benchmarks, highlighting its
practical beneﬁts in this domain. In the next section, we
detail our methodology, followed by an extensive experimen-
tal evaluation that substantiates the beneﬁts of our approach
over existing methods.
3 Methodology
In this section, we formalize the medical case retrieval
task and describe our LLM-augmented multi-agent retrieval
framework in detail. Our method consists of three main mod-
ules – the Query Planner (LLM Agent), the Retrieval Module,
and the Relevance Evaluator – that work together in an iter-
ative loop. We ﬁrst present the task deﬁnition and notation,
then explain the overall architecture with algorithmic pseu-
docode, and ﬁnally delve into each component’s technical
design, including model training objectives and integration
strategies. Key novel aspects, such as the multi-turn query
reﬁnement and LLM-based reranking, are highlighted.
3.1 Task definition and notation
We deﬁne medical case retrieval as follows: let D =
d1, d2,..., d|D| be a corpus of textual documents represent-
ing cases or articles (e.g. patient discharge summaries, case
reports, Q&A snippets). Given a query q which is a tex-
tual description of a patient’s case or an information need
(e.g. “55-year-old male with atypical chest pain and mild
troponin elevation”), the goal is to retrieve a ranked list of
documents [d
(1), d(2),..., d(k)] from D that are relevant to
q. We assume relevance is deﬁned in a task-speciﬁc way (for
instance, documents about patients with the same diagnosis
or containing useful insights for the query case). In an ideal
scenario, a ground truth set R
(q) ⊆ D of relevant items
exists for evaluation. The retrieval system’s job is to produce
an ordering of documents that places R
(q) at the top. For-
mally, we aim to learn a scoring functions(q, d) such that for
any two documents di, d j ,i f di is more relevant to q than d j ,
then s(q, di)> s(q, d j ). At query time, the system returns
the top- K documents by this score:
RetrieveTopK(q; D) = arg max
D′
∑
d∈D′
s(q, d) (1)
123

## Page 6

267 Page 6 of 19 Journal of King Saud University Computer and Information Sciences (2025) 37 :267
where D′ ranges over all size- K subsets of D. In prac-
tice, exhaustive maximization is infeasible for large D,s o
approximate methods (indexing, search algorithms) are used
to compute this top- K set efﬁciently.
A classical approach is to design s(q, d) as a similarity
measure (cosine similarity, dot product, etc.) between some
representation of q and d. Traditional IR uses sparse vectors
(based on term frequencies), while modern neural IR uses
dense vectors from learned encoders. Let f
θ(q) ∈ Rm and
gθ(d) ∈ Rm denote the query and document embeddings
produced by a neural bi-encoder model with parameters θ.
A common choice is:
sdense(q, d) =⟨ fθ(q), gθ(d)⟩, (2)
the dot product of the embeddings (or cosine similarity, which
is a scaled version if vectors are L2-normalized). The system
can then retrieve the top results by nearest-neighbor search
in the embedding space. We additionally consider the BM25
score as a complementary signal:
s
BM25(q, d) =
∑
t∈q
IDF(t) f (t, d)( k1 + 1)
f (t, d) + k1
(
1 − b + b |d|
avgdl
) ,
(3)
where f (t, d) is the term frequency of term t in d, and k1,
b are hyperparameters. This is the standard BM25 formula
capturing term matching with length normalization.
In our hybrid approach, we combine dense and sparse
scores:
shybrid(q, d) = λ· sdense(q, d)
∥ fθ(q)∥∥gθ(d)∥ + (1 − λ)· sBM25(q, d)
maxd′∈D sBM25(q, d′),
(4)
where we normalize each score and λ ∈[ 0, 1] controls the
weight (we setλ≈ 0.5 based on validation). The hybrid score
exploits both semantic matching from the neural model and
exact term evidence from BM25.
The task is complicated by the fact that q may not
be a perfect description of what to search for. Users (or
patients/clinicians) might omit details or use non-standard
terms. Thus, an implicit part of the task is query under-
standing and reformulation. We treat the initial query q
as potentially requiring transformation into an improved
query q
′ that yields better results. For example, q = "chest
pain with normal angiogram" might be reformulated to
q′ = "MINOCA (myocardial infarction with non-obstructive
coronary arteries) case". We denote a sequence of query
states q(0), q(1), ...,q(T) where q(0) is the original query and
q(T) is the ﬁnal reﬁned query after T iterations (or T can be
dynamically chosen when the system decides to stop). The
relationships between these queries and retrieved sets deﬁne
the multi-turn process.
3.2 Overall framework architecture
Figure 1 illustrates the architecture of our multi-agent
retrieval framework. The system comprises two primary
agents: (A) the LLM Planner Agent and (B) the Search/Re-
Fig. 1 The architecture of our multi-agent retrieval framework
123

## Page 7

Journal of King Saud University Computer and Information Sciences (2025) 37 :267 Page 7 of 19 267
trieval Agent, along with a subordinate module (C) the
Relevance Evaluator/Reranker which the LLM also controls.
The process works as follows:
• Initial Query & Encoding: The user’s query case q
(0) is
ﬁrst encoded by the retrieval agent’s encoder to produce
an initial embedding h(0) = fθ(q(0)). In parallel, the
LLM agent analyzes the raw query text and generates an
initial reasoning trace (e.g., it might internally parse the
query into key symptoms, duration, etc.).
• Initial Retrieval: The retrieval agent performs a search
in the case database D using the current query repre-
sentation. This could be a vector search using h
(0) to
ﬁnd nearest neighbors viasdense(q(0), d), optionally com-
bined with a BM25 search using q(0) terms. The result is
an initial candidate set C(0) = d(0)
1 ,..., d(0)
K with top-K
documents and their scores.
• LLM Analysis & Query Reﬁnement : The LLM plan-
ner agent receives the initial results C(0) (or summaries
thereof) plus the query. It evaluates the relevance: Are
the retrieved cases relevant or is the query missing some-
thing? The LLM uses a prompt template to produce an
output that can include:
– A brief analysis of the query and results (e.g., “The
query might be too broad, results span different dis-
eases.”).
– If needed, a reﬁned query q
(1) or a set of clarifying
questions. For instance, it might ask (to itself): “Is the
patient’s troponin high? If so, search for MINOCA
cases.” We implement this by having the LLM gener-
ate a new query string q
(1) in natural language (often
adding medical terms or synonyms gleaned from ini-
tial results).
– A termination decision: the LLM can also decide that
the results are satisfactory and no further iteration is
needed. This is encoded by a special output token
(e.g., the word “STOP” in the prompt).
• Iterative Loop : If not stopped, the new query q
(1) is
fed back to the retrieval agent, which re-encodes it and
performs a new search, yielding C(1). The LLM agent
reviews C(1) and may reﬁne again, producing q(2), and
so on. This loop continues for T iterations or until the
LLM explicitly signals to stop. In our implementation,
we cap T at 3 to 5 to avoid excessive searching, based on
development tuning – we found most queries converge
by 2 iterations.
• Final Ranking & Relevance Evaluation: Once the loop
ends at iteration T , we have a (hopefully improved) set
of candidates C(T). These are then passed through the
Relevance Evaluator, which can be a combination of the
LLM agent and a learned reranker. Speciﬁcally, for each
candidate d ∈ C
(T), we compute a ﬁnal relevance score:
sﬁnal(q(T), d) = α shybrid(q(T), d) + (1−α) sLLM(q(T), d),
(5)
where shybrid is the hybrid score from ( 3) using the lat-
est query embedding (this carries forward the IR model’s
judgment), and s
LLM(q, d) is a score assigned by the
LLM agent. How do we get sLLM?W ep r o m p tt h eL L M
with the pair (q(T), d) and ask it to output a relevance
rating. In practice, we ﬁne-tuned the LLM on a small set
of examples to output a probability or score 0–1 indicat-
ing relevance (details in Sec. 7.4). For efﬁciency, we only
rerank the top K from ﬁnal retrieval (e.g., K = 50). The
reranked top k (e.g., k = 10) are then returned as the
result list.
• Explanations: As a side beneﬁt, the LLM can produce
a natural-language explanation for the top results. Since
the LLM has essentially been reasoning about why doc-
uments are relevant, we capture those rationales. For
example, the LLM might output: “Document X is rel-
evant because the patient described also had chest pain
with normal coronaries, suggesting a similar MINOCA
scenario.” These explanations can be presented to the
user to increase trust in the results. While not a core
requirement of retrieval, we consider this an important
aspect of our framework’s design.
The above process is summarized in Algorithm 1 below.
In Algorithm 1, the loop of steps 2–7 embodies the coopera-
tive, iterative retrieval process. The LLM agent’s intelligence
guides the search agent, while the search agent provides the
LLM with grounding in actual data. This synergy is the crux
of our framework’s innovation. Next, we detail the compo-
nents: how the queries are reﬁned (prompt design), how the
retriever is built and trained, and how we train/integrate the
LLM’s relevance evaluation.
3.3 LLM planner agent: query reformulation and
reasoning
The LLM Planner Agent is implemented using a Transformer-
based large language model (speciﬁcally, we use a 7-
billion-parameter variant similar to LLaMA-2, chosen for its
manageable size and open availability). We ﬁne-tuned this
LLM on domain-speciﬁc instructions so that it can perform
the following sub-tasks:
• Summarize Query Context : The agent identiﬁes key
clinical facts in the query and potential synonyms or
related terms. For example, “troponin elevation” might
prompt it to recall terms like “cardiac enzyme rise”.
123

## Page 8

267 Page 8 of 19 Journal of King Saud University Computer and Information Sciences (2025) 37 :267
Algorithm 1 Multi-agent case retrieval (LLM-Planner +
Search Agent).
Input: Query text q(0), corpus D, LLM agent M, dense encoder fθ,
doc encoder gθ
Output: Ranked list of relevant documents
1: Set t = 0; receive initial query q(t) = q(0)
2: Encode query embedding h = fθ(q(t)) ⊿ Neural query
representation
3: Retrieve candidate set C(t) =
TopKδ
[
sdense(q(t), d) + sBM25(q(t), d)
]
for d ∈ D ⊿ Hybrid
retrieval using (4)
4: Query LLM M with prompt: “Query: q(t); Results: C(t) [summa-
rized]. Provide analysis.”
5: LLM outputs analysis and decision:
6: if LLM decides results are satisfactory or max iterations reached
then
7: goto Step 8
8: else
9: Let qnew = LLM’s reﬁned query string
10: Update t ← t + 1; set q(t) = qnew
11: goto Step 2
12: Final Scoring:
13: for each candidate d ∈ C(t) do
14: Compute sﬁnal(q(t), d) using (7) ⊿ IR score + LLM judgement
15: Sort C(t) by sﬁnal; return ranked list (and LLM explanations if avail-
able)
• Analyze Initial Results: After the ﬁrst retrieval, the LLM
is given titles or snippets of the top documents. We craft a
prompt that encourages a critical view: e.g., “The results
seem to be about X whereas the case might be about
Y. Perhaps the query is too general.” The LLM’s inter-
nal chain-of-thought (which we capture via prompting
techniques) might note something like: “Many results
are about angina, but our patient likely has microvascu-
lar issues. We should search for microvascular angina.”
• Decide to Reﬁne or Stop: The agent uses certain heuris-
tics (which we encoded in the prompt and reinforced
via ﬁne-tuning examples) to decide if another iteration
might yield better results. One heuristic is diversity: if
the top results are very heterogeneous or not focused on
the apparent key issue, a reﬁnement is likely needed. If
the results all look relevant and on-topic, it should stop.
We also check if the query is already very speciﬁc (e.g.,
containing a rare disease name); if so, fewer iterations
are used to avoid over-narrowing.
• Generate Reﬁned Query : When reﬁnement is needed,
the LLM produces a new query string. We guided
it to do this by showing examples during ﬁne-tuning
where a query was transformed: “young patient with MI
and clean coronary angiogram” - > “MINOCA case in
young patient”. The reﬁned query often includes med-
ical acronyms or more precise terms found in relevant
literature. In some cases, the LLM may break the query
into two sub-queries to retrieve different aspects; how-
ever, in our current design we handle one query at a time
(multi-query planning could be a future extension).
To realize these, we use a prompt template for the LLM
at each iteration, for example:
My Prompt
[System message:] Y ou are a medical research assis-
tant.
[User message:] Query: "55-year-old man, chest pain,
troponin 1.2, normal angiography."
Top results (titles):
1. "Case report: MINOCA presenting with chest
pain..."
2. "A study of microvascular angina in patients with
normal arteries..."
Analysis:
The LLM is expected to ﬁll in an analysis and perhaps sug-
gest a reﬁned query. We instruct it to output in a JSON-like
format, e.g., "decision": "reﬁne", "new_query": "...", "rea-
son": "..." for ease of parsing. If the decision is "stop", it can
optionally summarize why the current results are good.
We ﬁne-tuned the LLM on 50 crafted examples of
query-result-analysis triples, using domain knowledge from
clinicians to create reasonable reﬁnements. This was done via
low-rank adaptation (LoRA) so as not to modify the model
architecture. The LLM’s generation is constrained to avoid
introducing unrelated concepts – we found it occasionally
hallucinated irrelevant terms if not guided properly. There-
fore, we provided it with a list of known medical terms (from
UMLS ontology) to bias likely reﬁnements.
Mathematically, we can view the LLM’s reﬁnement step
as learning a functionQ
φ : (q(t), C(t)) ↦→ q(t+1), parameter-
ized by φ (the LLM weights). We don’t have an explicit loss
function for optimal q(t+1) (since what’s a “perfect” query
is subjective), but our ﬁne-tuning indirectly taught Qφ by
examples. One could also imagine optimizing this via rein-
forcement learning on retrieval performance (reward = AP
or NDCG of ﬁnal results), but that’s complex; we left it for
future work. Instead, we validate the efﬁcacy of Q
φ by the
improved metrics after reﬁnement (see Experiments).
3.4 Retrieval module: bi-encoder and index
The Retrieval Agent comprises a bi-encoder model for text
and an efﬁcient vector index for similarity search, augmented
with BM25 scoring. We implemented this using the FAISS
library for vector search (for dense embeddings) and Lucene-
based index for BM25 (for sparse terms).
Bi-encoder architecture We adopt a Siamese Transformer
encoder architecture. Speciﬁcally, we initialize it from a
biomedical pre-trained model to leverage domain language
123

## Page 9

Journal of King Saud University Computer and Information Sciences (2025) 37 :267 Page 9 of 19 267
patterns. In our experiments, we tried BioBERT and Pub-
MedBERT as initialization and found PubMedBERT (which
is BERT-base trained on PubMed abstracts) slightly bet-
ter. Let E
BERT(x) denote the ﬁnal [CLS] embedding of
input text x. We use two such encoders with shared weights
for queries and documents, i.e., fθ(q) = EBERT(q) and
gθ(d) = EBERT(d). The shared θ ensures the query and
doc live in the same vector space. We add a linear projection
and L2 normalize the vectors to encourage cosine similarity
learning:
hq = W[EBERT(q)]
∥W[EBERT(q)]∥, hd = W[EBERT(d)]
∥W[EBERT(d)]∥, (6)
where W is a learned matrix projecting BERT’s hidden
size to the embedding dimension (which we set to 256
for efﬁciency). We use dot-product between hq and hd as
sdense(q, d).
Training the bi-encoder We create training pairs from avail-
able data. Each query (case description) has some known
relevant documents (cases or articles) from either annota-
tions or distant supervision. For example, in the MIMIC-III
dataset we derive pseudo-relevant documents by consid-
ering patients with the same diagnosis codes as relevant
to each other’s descriptions. In the PMC-Patients dataset,
we directly have patient–patient similarity annotations and
patient–article links, which we use for training. We construct
triples (q, d
+, d− ) where d+ is relevant and d− is a random
irrelevant document. We then optimize a contrastive softmax
loss (also known as negative log-likelihood for the correct
doc or InfoNCE):
Ldense(θ)=− 1
N
N∑
i=1
log
exp(sim(qi , d+
i ))
exp(sim(qi , d+
i )) + ∑M
j=1 exp(sim(qi , d−
i, j ))
,
(7)
where each query qi is paired with one positive d+
i and M
negatives d−
i, j . We mine hard negatives by using an initial
BM25 ranking and taking top non-relevant docs as well as
random negatives. This loss encourages the model to give
a higher score to relevant pairs than to any irrelevant pair.
Intuitively, it tries to satisfy sim (q, d
+)> sim(q, d− ) by
a margin. We trained with M = 15 negatives per positive
in-batch (using in-batch examples as negatives as well as
explicit hard negatives). The ﬁnal model (after ﬁne-tuning
for 3 epochs on each dataset) signiﬁcantly outperforms an
untrained BioBERT on retrieval (see Experiments).
The output of this module during inference is the list of
top K docs by s
hybrid(q, d) (3). We empirically set K = 50
for initial retrieval (which the LLM sees summarized) and
K = 100 for ﬁnal retrieval before reranking (to ensure we
give the LLM plenty of candidates to consider). We also
incorporate BM25 with RM3 expansion for the initial query:
using the top retrieved documents, we expand the query with
top terms (RM3 algorithm) to form a hybrid query vector.
This gave a small boost in recall for the ﬁrst iteration, ensur-
ing the LLM sees a diverse set of results to work with. We
note that techniques like SPLADE, which learn sparse expan-
sion terms via transformers, could further improve our sparse
component; we leave exploring advanced sparse models (e.g.
SPLADE, uniCOIL) to future work, as our focus is on the
interaction mechanism.
3.5 LLM relevance evaluator and reranking
Once the iterative search concludes, we have a set of candi-
dates C(T) that are likely relevant. However, their initial ranks
might not be optimal, because the bi-encoder is optimizing
mostly for recall (it’s a shallow semantic match). Therefore,
we add a ﬁnal re-ranking step. Traditional IR pipelines often
use a cross-encoder (BERT-based classiﬁer that reads the
query and doc together) to rerank top candidates with higher
precision. We take a similar approach but leverage the LLM
agent for this purpose, yielding both a score and an explana-
tion for each document.
LLM Relevance Scoring: We ﬁne-tuned the same LLM on
a small supervised dataset for binary relevance classiﬁcation.
We used about 1000 query-doc pairs annotated (or inferred)
as relevant or not (500 each) from a combination of:
• TREC-COVID and TREC-CDS judgments for some
queries.
• PMC-Patients dataset’s similarity labels.
• A handful of expert-labeled cases we created for valida-
tion.
We formatted each as: "[QUERY] – [DOC TITLE]: rel-
evant (yes/no)?" and ﬁne-tuned the LLM to output "yes" if
relevant, "no" if not. We achieved 90% accuracy on a val-
idation set of this task, which was sufﬁcient for our needs.
During inference, for eachd in C
(T),w ea s kt h eL L M :" Q u e s -
tion: [q]. Is the following text relevant? [d]. Respond ’yes’ or
’no’." Internally, we actually prompt it to output a probabil-
ity (by asking it to rate on a 1–5 scale and mapping to 0–1).
The output is parsed into a score s
LLM(q, d). Because this
involves reading the document text (or at least a summary
of it), it’s more expensive than the bi-encoder’s dot product.
But since |C
(T)|= 100 at most, it’s manageable. In fact, we
further optimize by ﬁrst ﬁltering obvious non-relevant via the
bi-encoder score: we only send, say, the top 50 by s
dense to
the LLM for scoring to save time.
Combining scores We learned the weightα in (7)o nad e vs e t
by grid search. It turned out giving a slight edge to LLM score
123

## Page 10

267 Page 10 of 19 Journal of King Saud University Computer and Information Sciences (2025) 37 :267
(α ≈ 0.4) worked well – meaning the LLM’s judgment can
overturn the bi-encoder rank for some items, which improved
precision. Essentially, if the LLM strongly says "this is rel-
evant", we trust it even if that doc was slightly lower in the
dense rank. If the LLM is unsure, the dense model’s ordering
prevails. This ensembling is similar in spirit to hybrid rankers
in IR that mix lexical and semantic signals, but here we mix
learned and generative signals.
Algorithmic complexity Each iteration involves one vector
search (fast, milliseconds with FAISS) and one LLM forward
pass generating a short text (taking 1–2 seconds with a 7B
model on our hardware). We allow up to 3 iterations, so 3
LLM calls per query for planning, plus up to 50 short LLM
calls for reranking. This is heavier than a standard search
engine but still within practical limits for many use-cases (a
few seconds per query). One could reduce cost by using a
smaller model for reranking (like a MiniLM cross-encoder),
but we found the LLM’s ﬂexibility useful for explanation.
4 Experiments
We conducted extensive experiments to answer the follow-
ing questions: (1) Effectiveness: Does our LLM-augmented
multi-agent retrieval method outperform existing retrieval
methods on medical case retrieval tasks? (2) Ablation: How
much do different components (LLM query reﬁnement, LLM
reranking, hybrid scoring) contribute to the performance?
(3) Generality and Robustness: Does the method general-
ize across different datasets and remain robust to variations
such as query noise or cross-domain application? (4) Efﬁ-
ciency and Scalability: What is the runtime overhead of the
multi-agent approach, and is it feasible for practical use? (5)
Explainability: Does the LLM agent provide useful explana-
tions, and how do users perceive them (though this last point
we address qualitatively)?
4.1 Datasets
We evaluated on three open-source datasets that cover a spec-
trum of medical text retrieval scenarios:
• MIMIC-III Case Retrieval (Johnson et al. 2016): We
derived a retrieval task from the MIMIC-III critical care
database. MIMIC-III contains de-identiﬁed electronic
health records for ICU patients, including discharge sum-
maries. We constructed 100 queries by taking discharge
summaries of patients as query documents (after remov-
ing identifying info) and aimed to retrieve other patient
records with similar diagnoses. We used ICD-9 diagnos-
tic codes as a proxy for relevance: if two patients share
a primary diagnosis code (e.g., myocardial infarction),
their documents are considered relevant to each other.
On average, each query had 8 relevant records among
a corpus of 10,000 summaries (we sampled a subset
of MIMIC for manageability). This dataset tests case-
to-case retrieval on long, unstructured EHR notes. It is
challenging due to vocabulary (medical jargon, abbrevia-
tions) and the length of documents (often >1000 words).
• TREC-COVID IR Challenge (Roberts et al. 2020): The
TREC-COVID dataset was a shared task in 2020 with
topics (queries) about COVID-19 and a corpus of scien-
tiﬁc articles (from CORD-19). We chose this to evaluate
our method’s performance on case to literature retrieval
under a speciﬁc context. We used the 35 query topics from
Round 5 of TREC-COVID (e.g., “impact of COVID-19
on mental health”). Although not patient cases per se,
these are medical information needs expressed in natural
language. The corpus has 60k articles. Relevant arti-
cles per query were judged by TREC assessors (mean
40 per query). This dataset tests how our method han-
dles broad biomedical questions and whether the LLM’s
world knowledge (which might include some COVID
info up to 2021) helps or causes issues. It’s also a test of
handling a large corpus efﬁciently.
• PMC-Patients (Clinical Case Dataset) (Zhao et al.
2023): PMC-Patients is a recently released large dataset
of patient summaries extracted from PubMed Central
articles, with annotated relevance relations. It directly
provides two tasks: (a) Patient-to-Patient Retrieval (PPR)
– ﬁnd similar patient cases; and (b) Patient-to-Article
Retrieval (PAR) – ﬁnd relevant journal articles given a
patient case. We focus on the PPR task since it aligns with
case retrieval. The dataset has 167k patient summaries;
among them 293k pairs are marked as similar (derived
via shared citations in the original articles). We sampled a
subset of 5,000 queries and 50,000 corpus cases for eval-
uation due to resource limits (ensuring each query had
at least 1 relevant). This is by far the largest-scale and is
a true case narrative retrieval task. Many cases are long
and detailed, and relevance is deﬁned by expert insight
(e.g., whether two patients in case reports were consid-
ered analogous by the authors). The size also tests the
scalability of our approach.
These three datasets collectively cover a wide spectrum
of medical retrieval scenarios: MIMIC-III focuses on EHR-
based case-to-case retrieval; TREC-COVID targets topical
medical literature search under time-sensitive public health
crises; and PMC-Patients provides patient-to-patient narra-
tive matching at scale. Together, they represent structured and
unstructured data, formal and colloquial language, and nar-
row versus broad information needs, offering a robust testbed
without redundancy.
123

## Page 11

Journal of King Saud University Computer and Information Sciences (2025) 37 :267 Page 11 of 19 267
Data preprocessing For each dataset, we preprocessed text
by lowercasing, removing obvious de-identiﬁcation artifacts
(like “[Name]”), and splitting any very long documents
(>1500 words) into multi-part documents (to avoid huge
input length for BERT/LLM). In PMC-Patients, summaries
were already reasonably sized ( 400 words average). For
MIMIC, we truncated or split at 800 words for indexing,
with a pointer so that if one part is retrieved, we know which
record it belongs to (for evaluation). We used NLTK and
spaCy for basic tokenization when needed (for BM25 index-
ing). The LLM agent’s input was limited to 300 tokens for
result summaries (we didn’t feed entire documents to it in the
reﬁnement stage, just titles or key sentences, to keep prompts
concise).
4.2 Baseline methods
We compared our approach (Multi-Agent LLM Retrieval,
denoted Ours) with a comprehensive list of baseline methods
spanning classical, neural, and LLM-based approaches:
• BM25 (Robertson et al. 2009): The standard Okapi
BM25 ranking using Lucene’s implementation. This is
a strong baseline in text retrieval, including medical IR
tasks, and often hard to beat without large training data.
• BM25+RM3 (Robertson et al.2009): BM25 with pseudo-
relevance feedback (RM3) expansion. We expand each
query with top 10 terms from the top 10 initial results
(common setting). This often improves recall in medical
search by adding synonyms.
• Doc2V ec(Le and Mikolov 2014): Unsupervised dense
embedding of documents using the PV -DBOW model.
We trained a Doc2V ec on each corpus (vector size 200).
Queries are embedded by averaging word vectors (since
Doc2V ec doesn’t directly give query embeddings). Sim-
ilarity by cosine. This represents early neural IR without
supervision.
• Sentence-BERT (MiniLM) (Reimers and Gurevych
2019): As a strong semantic baseline, we used a pre-
trained Sentence-BERT model (speciﬁcally, all-MiniLM-
L6-v2, a 6-layer 22M parameter model trained on general
sentence pairs). We encoded query and documents with
it and did cosine similarity search. This model is not
domain-speciﬁc but captures general semantic similar-
ity.
• BioBERT Dense (Lee et al. 2020): We ﬁne-tuned
BioBERT (BERT-base architecture) as a bi-encoder on
each dataset’s training queries (where available). This
baseline mirrors our retrieval model but without any
LLM involvement or multi-turn ability. Essentially, it’s
our bi-encoder alone. We include this to see how much
improvement comes from multi-agent and LLM compo-
nents beyond a well-trained dense retriever.
• ColBERT (Khattab and Zaharia 2020): We adapted the
ColBERT late-interaction model. We used a publicly
available checkpoint trained on MS MARCO, and then
further ﬁne-tuned it on our tasks (for those with training
data). ColBERT uses contextualized token embeddings
and max-sim scoring, which can capture ﬁner relevance
at the cost of index size. We evaluate it to see if ﬁne-
grained matching helps more than our approach.
• ANCE (LMIR) (Xiong et al. 2020): We include ANCE
(Approximate Nearest Cluster Encoder), a state-of-the-
art unsupervised dense retrieval method. ANCE itera-
tively updates representations by mining hard negatives.
We use an ANCE model trained on MS MARCO (since
domain-speciﬁc ANCE wasn’t available) as a zero-shot
baseline. This tests a scenario of a powerful general
retriever without domain adaptation.
• GPT-3.5 (Closed-book): To assess an LLM-only approach,
we used OpenAI’s GPT-3.5 (text-davinci-003) in a
closed-book manner. For each query, we prompted:
“Retrieve relevant cases or studies about: [query]. List
titles or IDs.” Essentially asking it to recall from its
training knowledge. Since it cannot access the corpus, it
either produces some plausible titles or admits it doesn’t
have the data. We wanted to measure how often it can
“retrieve” known cases correctly. We evaluated its out-
put by matching to relevant documents (if it named an
article, we check if that article is relevant and in the cor-
pus). This baseline illustrates the limitation of not using
an external knowledge base.
• GPT-3.5 + RAG (One-shot): Here we simulate a single-
turn RAG: we use BM25 to retrieve top 5 documents for
the query, then feed their content (a brief summary each)
along with the query into GPT-3.5, prompting: “Given the
query and these documents, which documents are rele-
vant?”. We formatted documents as [Doc ID: excerpt].
GPT-3.5 then outputs a list of relevant Doc IDs. We mea-
sure precision/recall of that selection. This represents a
pipeline where a strong LLM is used to ﬁlter/rank one
batch of retrieved docs, but without iterative reﬁnement.
• Single-turn Ours (No iteration) : This ablation is basi-
cally our system if we disable query reﬁnement. The LLM
is only used for ﬁnal reranking. The query goes once to
the retriever (which uses the same bi-encoder), and then
LLM scores and ranks. This helps isolate the impact of
the iterative aspect.
All methods that produce a ranking are evaluated using the
same metrics on the same sets. For neural baselines requiring
ﬁne-tuning (BioBERT, ColBERT), we used 70% of queries
for training, 10% for validation, 20% for test (for MIMIC and
PMC datasets). For TREC-COVID, which has no training
123

## Page 12

267 Page 12 of 19 Journal of King Saud University Computer and Information Sciences (2025) 37 :267
set (it was an evaluation benchmark), we used the provided
judgments only for test; BioBERT was ﬁne-tuned on a com-
bination of MS MARCO and some COVID-QA data as a
proxy. Our method does not require query training (except
minimal for LLM which we did generically), so it’s evaluated
on test queries directly.
4.3 Evaluation metrics
We primarily report Recall@K (with K=10 and 20), NDCG@10,
and MAP (Mean Average Precision) for each method on
each dataset. Recall@K is crucial in case retrieval because
missing a truly relevant case could be serious. NDCG and
MAP reﬂect ranking quality (NDCG@10 emphasizes the top
results, MAP considers the entire ranking). We also measure
Precision@10 to see speciﬁcity. For TREC-COVID (where
relevance judgments have graded scores), NDCG is par-
ticularly appropriate, but we treat it as binary relevant for
simplicity (relevant vs not) in this evaluation.
Additionally, we perform statistical signiﬁcance testing
between our method and baselines using a paired t-test on
per-query average precision, with p < 0.05 considered sig-
niﬁcant improvements.
4.4 Implementation details
All experiments were conducted on a machine with 4 ×
NVIDIA A100 GPUs (40GB each), with total training time
of approximately 20 GPU hours per dataset (bi-encoder train-
ing). The LLM agent (7B parameter model) was ﬁne-tuned
using LoRA for 3 hours per task, and each query took on
average 3–5 seconds end-to-end during inference, including
iterative planning and reranking. Throughput during infer-
ence was approximately 10 queries per minute when batched.
The average number of tokens processed per query
(including prompt + retrieved text) was 800–1200 tokens per
iteration. Given the cap of 3 iterations per query and a batch
reranking of top-50 candidates, the total token consumption
per query remained under 4,000 tokens.
These settings balance model performance with reason-
able compute costs for real-world use.
4.5 Main results
Table 1, 2, and 3 presents the main results on the test sets of
the three datasets. We show the performance of each method
across several metrics (averaged over queries).
Several observations from Table:
• Our full method (Ours multi-turn) achieves the highest
scores on all metrics in all three datasets. For example, on
MIMIC, we attain NDCG@10 = 0.501, which is a 5%
absolute gain over the best baseline (ColBERT at 0.452)
Table 1 Performance on MIMIC
Method NDCG@10 Rec@10 MAP
BM25 0.392 0.500 0.321
BM25 + RM3 0.401 0.537 0.330
Doc2V ec 0.288 0.402 0.220
Sentence-BERT 0.412 0.575 0.345
BioBERT Dense (FT) 0.440 0.612 0.369
ColBERT (FT) 0.452 0.620 0.377
ANCE (zero-shot) 0.398 0.580 0.330
GPT-3.5 closed-book 0.105 0.150 0.080
GPT-3.5 + RAG (1-shot) 0.430 0.600 0.360
Ours (single-turn) 0.473 0.645 0.389
Ours (multi-turn) 0.501 0.702 0.421
and 6% over BioBERT dense. Recall@10 is particu-
larly improved: 70.2% versus 62.0% for ColBERT – a
signiﬁcant boost in ﬁnding relevant cases. Similar trends
are seen in TREC-COVID (NDCG@10 +4.4 points over
best baseline) and PMC-Patients (+6.2 points). These
improvements of 4–8 percentage points conﬁrm that our
method meets the expected performance gain range out-
lined (we targeted +4–6%, and observed even slightly
more in some cases). Statistically, paired tests showed
p<0.001 in most comparisons, indicating high conﬁ-
dence in the improvement.
• Among baselines, neural methods outperform BM25
variants consistently. BioBERT dense and ColBERT ﬁne-
tuned have an edge over BM25 by 4-5 points NDCG,
showing the value of semantic understanding in these
tasks. ColBERT slightly outperforms BioBERT dense,
likely due to its ﬁner grained matching – interestingly,
that gap is narrower on PMC (0.422 vs 0.410 NDCG)
suggesting that in a very domain-speciﬁc setting, a well-
trained bi-encoder is nearly as good. Sentence-BERT
Table 2 Performance on TREC-COVID
Method NDCG@10 Rec@10 MAP
BM25 0.447 0.534 0.358
BM25 + RM3 0.459 0.590 0.370
Doc2V ec 0.310 0.421 0.250
Sentence-BERT 0.462 0.601 0.376
BioBERT Dense (FT) 0.480 0.625 0.389
ColBERT (FT) 0.491 0.638 0.398
ANCE (zero-shot) 0.470 0.610 0.380
GPT-3.5 closed-book 0.132 0.100 0.090
GPT-3.5 + RAG (1-shot) 0.475 0.630 0.387
Ours (single-turn) 0.507 0.678 0.411
Ours (multi-turn) 0.535 0.735 0.438
123

## Page 13

Journal of King Saud University Computer and Information Sciences (2025) 37 :267 Page 13 of 19 267
Table 3 Performance on PMC-Patients
Method NDCG@10 Rec@10 MAP
BM25 0.351 0.472 0.290
BM25 + RM3 0.366 0.506 0.301
Doc2V ec 0.275 0.398 0.210
Sentence-BERT 0.384 0.542 0.315
BioBERT Dense (FT) 0.410 0.587 0.342
ColBERT (FT) 0.422 0.595 0.350
ANCE (zero-shot) 0.376 0.560 0.308
GPT-3.5 closed-book 0.050 0.070 0.045
GPT-3.5 + RAG (1-shot) 0.402 0.580 0.335
Ours (single-turn) 0.445 0.610 0.361
Ours (multi-turn) 0.484 0.658 0.392
(without domain ﬁne-tuning) already beats BM25 as
well, highlighting how much semantic matching mat-
ters for case retrieval. The ANCE zero-shot model did
fairly well too, better than BM25 but slightly below the
in-domain ﬁne-tuned ones, indicating that some domain
mismatch still hurts (it was trained on general web QA).
• The LLM-based baselines are telling. GPT-3.5 in closed-
book mode performed very poorly (NDCG 0.1). It often
returned hallucinated case references or very generic
statements. For example, for a lupus query, it said “This
could be a case of lupus ﬂare vs infection” but didn’t
retrieve anything concrete. This underscores that an LLM
without retrieval is not an effective search engine. The
GPT-3.5 + RAG one-shot baseline improved things dra-
matically by adding retrieval: its scores approach those of
ﬁne-tuned neural models (NDCG 0.430–0.475 range). In
fact, GPT-3.5 + RAG slightly beat BioBERT on MIMIC
and was on par on others. This suggests that an LLM
can effectively rerank a small set of docs when given to
it, which aligns with observations in other RAG work.
However, it still underperforms our method by a few
points. The likely reason is that our method’s iterative
querying ﬁnds documents that the initial retrieval missed
(improving recall), and the LLM’s targeted reﬁnement
yields more focused results. Also, our LLM (ﬁne-tuned
and smaller) might actually be more specialized for this
task than GPT-3.5 which is very general. Another factor
is that GPT-3.5 sometimes made mistakes like wrongly
dismissing a relevant doc if it wasn’t obviously on topic at
ﬁrst glance; our multi-turn approach could reﬁne queries
to bring more obviously relevant docs.
• Comparing Ours (single-turn) vs Ours (multi-turn), we
see the beneﬁt of the multi-turn iterative process. Single-
turn (which is basically “dense retrieval + LLM rerank
once”) is already competitive, beating most baselines
(due to the strong bi-encoder and LLM reranker). But
multi-turn adds another 3 points NDCG and signiﬁcant
recall gain. For instance, on PMC-Patients, single-turn
recall@10 was 0.610, multi-turn pushed to 0.658 – mean-
ing on average an extra 4-5 relevant cases retrieved per
100 queries that would otherwise be missed. This demon-
strates that the query reﬁnement loop is indeed fetching
additional relevant documents that were not retrieved ini-
tially. On manual inspection, many of these were cases
where the initial query used a less common term or was
missing a key term that the LLM added in reﬁnement,
pulling in those docs.
• The improvements are most pronounced on PMC-
Patients (the largest and perhaps hardest set). A 6+ point
NDCG jump over best baseline indicates our approach
scales well with data complexity. On TREC-COVID,
improvements are slightly smaller (but still 4 points).
One reason: TREC-COVID queries are more straight-
forward (often a single well-deﬁned question) so there’s
less room for query reﬁnement; nonetheless, we still
saw beneﬁt (the LLM might add synonyms or related
concepts e.g., “SARS-CoV -2” for “COVID-19” which
BM25 already handles somewhat but the LLM ensured
relevant subtopics were included). On MIMIC, which
has extremely diverse free-text notes, the method shines
in recall..
In summary, the overall performance conﬁrms that our
multi-agent LLM retrieval method outperforms state-of-the-
art baselines, achieving both higher recall and precision.
Next, we analyze speciﬁc aspects via ablations and additional
experiments.
4.6 Ablation study
We perform a series of controlled experiments to isolate the
contributions of different components of our system:
Ablation A Removing LLM Query Reﬁnement. We already
saw the effect in the single-turn vs multi-turn comparison.
To quantify further, we measure the performance after each
iteration for our method. For example, at iteration 0 (no
reﬁnement) vs after 1 reﬁnement vs after 2 reﬁnements.
• On MIMIC, recall@10 starts at 0.62 (iteration 0), goes
to 0.68 after one LLM reﬁnement, and 0.70 after the sec-
ond. NDCG similarly rises from 0.47 to 0.50 to 0.501.
The gains from iteration 0→ 1 are larger than 1→ 2, indi-
cating diminishing returns (most queries only needed one
reﬁnement). By iteration 2, many queries had the LLM
decide to stop anyway. We rarely needed a third iteration
(the curve plateaued).
• On PMC-Patients, a similar pattern: recall@10 from 0.56
→ 0.63 → 0.658 after two iterations; NDCG from 0.44
→ 0.47 → 0.484. This shows iterative querying is bene-
123

## Page 14

267 Page 14 of 19 Journal of King Saud University Computer and Information Sciences (2025) 37 :267
ﬁcial particularly for difﬁcult cases that the initial query
didn’t hit perfectly. Qualitatively, we saw the ﬁrst itera-
tion often adds obvious synonyms or related conditions,
and the second iteration sometimes explores a secondary
aspect (like a complication of a disease, if initial and
reﬁned queries both still returned mixed results).
These results justify our decision to allow up to 2 itera-
tions. We set a hard limit of 3 in code, but it was almost never
used (only 2% of queries did a third, and improvements were
negligible or they were edge cases).
Ablation B Removing LLM Reranker. We tested a version
of our system where we do everything (including multi-turn
reﬁnement) but do not use the LLM for ﬁnal reranking (and
no s
LLM in (7)). Instead, we simply take the top results from
the bi-encoder (hybrid score) after the ﬁnal iteration. The per-
formance dropped by around 2 points NDCG and 3 points
precision@10 across datasets. For instance, on MIMIC, with-
out LLM rerank we got NDCG@10 ≈ 0.483 (vs 0.501
with it) and precision@10 0.50 (vs 0.54). The LLM’s value
here is in ﬁne-tuning the ordering at the top: it occasion-
ally demoted false positives that the bi-encoder thought were
similar but in fact had subtle differences making them irrel-
evant (the LLM caught those by reading content). It also
promoted some relevant docs that bi-encoder scored slightly
lower due to vocabulary mismatch (essentially acting like a
cross-encoder). Thus, the reranker LLM contributes, though
its impact is smaller than the query reﬁnement’s impact on
recall.
Ablation C Disabling Hybrid (Using Dense Only). Our
method uses hybrid dense+BM25 scoring for initial retrieval.
If we remove the BM25 part and rely purely on dense
scores for candidates, performance dropped slightly in recall
(by 1-2 points) on MIMIC and PMC. The dense model,
while trained, sometimes misses very speciﬁc terms (e.g.,
medication names or rare symptoms that are better treated
as keywords). BM25 helped ensure those weren’t missed.
However, the effect was not huge, indicating our dense
model learned a lot of the term importance as well. On
TREC-COVID, interestingly, dense-only was almost equal to
hybrid, likely because queries were well-formed and models
like BioBERT already know those terms.
Ablation D V arying LLM Agent Size. We experimented with
a smaller LLM (2.7B parameters) versus a larger (13B) to see
the effect. The 2.7B (ﬁne-tuned similarly) resulted in about
1.5 points lower NDCG and noticeably worse explanations
(sometimes incorrect). The 13B gave slightly better reason-
ing but was slower; its retrieval performance was on par with
7B (within 0.3 points on metrics). We chose 7B as a sweet
spot for speed and performance. This suggests that extremely
large models (like GPT-3.5 with 175B) are not necessary if
you ﬁne-tune a moderate LLM on domain tasks – a positive
sign for deployability.
Ablation E Prompt V ariations. We tested a variant prompt
where the LLM was not asked to explicitly analyze results
(just directly suggest a new query). That led to more erratic
reﬁnements, sometimes missing the reason to reﬁne. Includ-
ing a step where it “explains to itself” in the prompt yielded
more consistent improvements. This aligns with chain-of-
thought research that letting the model articulate reasoning
leads to better decisions. We also tried an automatic stopping
criteria by checking if top documents’ scores exceed a thresh-
old (instead of LLM saying STOP). That was less reliable, as
some queries needed stopping even if score threshold not met,
because further reﬁnement could have over-specialized. We
kept the LLM’s own judgment for stopping, which worked
well in our ﬁne-tuned model (it stopped appropriately 90%
of time; in 10% we noticed it could perhaps reﬁne one more
s t e pb u td i d n ’ t ,w h i c hi sa na r e at oi m p r o v e ) .
4.7 Statistical and robustness analysis
To assess the statistical signiﬁcance of performance gains, we
report 95% conﬁdence intervals (CI) for core metrics based
on bootstrapped sampling over test queries. For instance, our
method achieves an NDCG@10 of 0.501± 0.014 on MIMIC,
compared to 0.452 ± 0.012 for the best baseline (ColBERT),
with a paired t-test p-value < 0.01.
Moreover, per-query breakdowns conﬁrm consistency:
across all 100 queries in MIMIC, 87 showed improved
NDCG@10 with our method, and 61 queries improved by
>5%. Similar trends were observed on PMC-Patients.
To evaluate robustness to iteration depth, we compared
performance across different maximum iteration caps (T=0
to T=3). The gains mostly materialized within the ﬁrst two
iterations, with marginal returns beyond T=2. This supports
our design choice to cap iterations at T=3 while ensuring
efﬁcient search.
4.8 Generalization and robustness experiments
Cross-dataset transfer We wanted to see if our approach,
trained (ﬁne-tuned) on one type of data, could apply to
another without retraining – a measure of generalization.
We took our model (bi-encoder + LLM agent) trained
on PMC-Patients and applied it zero-shot to the TREC-
COVID queries. Surprisingly, it still outperformed BM25
(NDCG@10 0.50 vs 0.447 BM25). It was slightly behind
the model tuned with some COVID data (which got 0.535 as
in Table 1). The LLM agent correctly reﬁned some COVID
queries by adding terms like “SARS-CoV -2” or known drug
123

## Page 15

Journal of King Saud University Computer and Information Sciences (2025) 37 :267 Page 15 of 19 267
names, which it presumably learned from its pretraining or
from the general medical ﬁne-tuning. However, for very spe-
ciﬁc queries (like “effect of ACE2 polymorphism on COVID
outcomes”), the model didn’t have training analogs and strug-
gled to reﬁne usefully. This suggests our approach has some
transfer ability thanks to the LLM’s broad knowledge and
the general nature of bi-encoder training on large text, but
speciﬁc tuning still helps. Similarly, applying the MIMIC-
trained model to PMC (zero-shot) gave NDCG 0.45 vs 0.484
when trained on PMC – a small drop.
Robustness to noisy queries Clinical queries can be noisy
or incomplete. We simulated noise by taking 20 queries
from MIMIC and adding irrelevant sentences (e.g., “Patient’s
favorite color is blue.”) or typos (random character swaps) to
them. We then ran BM25 and our method on these perturbed
queries. BM25’s precision dropped signiﬁcantly (it got dis-
tracted by odd terms like “blue”, retrieving some unrelated
docs), and recall dropped slightly. Our method’s LLM agent
was generally able to ignore irrelevant text; in its analysis
it often didn’t even mention the added sentence, focusing
on medical content. As a result, our performance remained
within 1% of original on those queries, whereas BM25’s
NDCG fell by 5%. This highlights that the LLM provides a
degree of robustness to noise, effectively ﬁltering out irrele-
vant information from the query. This is a practical advantage
in real-world use where queries may contain extraneous info
or user errors.
Robustness to Out-of-Vocabulary (OOV) terms We also
tested queries containing a made-up drug name and a rare
disease acronym that wasn’t in training data. The LLM agent
asked (in reﬁnement) for clariﬁcation of the acronym in one
case, essentially splitting it out (not that it can truly clarify
with user, but it rephrased query to use full form it guessed).
For the made-up drug, both BM25 and our method couldn’t
ﬁnd it (no doc had it), but our method still retrieved rele-
vant docs by focusing on the rest of query, whereas BM25
returned nothing in top ranks because it was too anchored on
the rare term. This again demonstrates the resilience provided
by semantic understanding and ﬂexible reasoning.
Efﬁciency On a single Nvidia A100 GPU, our end-to-end
query processing took on average 2.8 seconds (including up
to 2 LLM calls and all retrieval steps) for MIMIC queries,
and 3.5 seconds for PMC queries (which have longer text
to embed and more to rerank). Baseline neural methods like
ColBERT took 1.5 seconds (mostly embedding + nearest
neighbor search with no heavy model afterward). So our
approach is slower, but still interactive for moderate use and
could be optimized (e.g., pipeline the retrieval and LLM com-
putations, use quantization on LLM). Notably, using GPT-3.5
via API was much slower ( 10–15 s per query) and costlier;
our ﬁne-tuned local LLM was more efﬁcient. There is a trade-
off: multi-step reasoning will inherently add latency, but we
believe the accuracy gains justify it for scenarios where thor-
ough retrieval is needed (like clinical research, where a few
seconds per query is acceptable). In a live clinical setting
with high frequency queries, further optimization or distilla-
tion would be needed.
4.9 Case study and explainability analysis
To qualitatively assess explainability, we present a case from
the PMC-Patients dataset and the outputs of different sys-
tems:
Query case (excerpt) “A 34-year-old male with a history
of IV drug use presents with fever and weight loss. Blood
cultures grow gram-positive rods. Imaging reveals endo-
carditis. He is diagnosed with infective endocarditis due to
Corynebacterium striatum.”
The task is to retrieve similar patient cases. Key aspects:
young male IV drug user, unusual pathogen (Corynebac-
terium striatum) causing endocarditis.
• M25 returned some general endocarditis articles (mostly
on Staphylococcus aureus endocarditis in IV drug users –
relevant to IV drug use but different bacteria) and missed
cases about Corynebacterium (because the query didn’t
explicitly say “Corynebacterium” in early part maybe).
• Our method ﬁrst iteration likely added focus: seeing
“gram-positive rods endocarditis”, the LLM might reﬁne
query to “endocarditis IV drug user Corynebacterium
case”. It did retrieve a case report of “Endocarditis by
Corynebacterium in an IV drug user” and another “Non-
diphtherial Corynebacterium endocarditis – case series”.
These were highly relevant (they discuss the same rare
pathogen in similar context).
• The LLM’s explanation for the top result: “Relevant:
Both the patient in the query and in Document 1 are IV
drug users who developed infective endocarditis from a
rare Corynebacterium species, which is an unusual cause
of endocarditis. This directly matches the case.” This
Table 4 Clinician ratings of
explanation quality (Scale: 1–5) Metric Mean Score (Ours) Mean Score (BioBERT) Std. Dev.
Relevance 4.7 2.8 ±0.4
Faithfulness 4.5 2.6 ±0.5
Usefulness 4.8 2.4 ±0.3
123

## Page 16

267 Page 16 of 19 Journal of King Saud University Computer and Information Sciences (2025) 37 :267
kind of explanation not only tells us the document is
relevant but why – citing the shared uncommon factor
(Corynebacterium in IV drug context). This is valuable
t oau s e rt ot r u s tt h er e s u l t .
• For a less relevant result (like one BM25 gave about Staph
aureus), the LLM (when used in rerank) down-scored
it and we saw an explanation like: “Partial match: IV
drug use is similar, but the pathogen differs (Staphylococ-
cus vs Corynebacterium), so it’s not the same scenario.”
This nuance shows the system’s ability to distinguish ﬁne
details – something pure embedding models might strug-
gle to articulate.
We also conducted a small user study with 3 clinicians,
showing them blind outputs: one from BioBERT baseline
(just a list of titles) and one from our system (titles + our
LLM explanations), for 5 queries. All 3 preferred our sys-
tem’s outputs, citing that the explanations made it clear why
each result was chosen, and often taught them something
(e.g., if they weren’t aware of a certain pathogen or associ-
ation, the explanation highlighted it). They also pointed out
if an explanation didn’t make sense, they would know the
result might be a miss. This suggests that the explainabil-
ity could also serve as a conﬁdence indicator. While this is
anecdotal, it reinforces the importance of the “agent’s com-
mentary” beyond just retrieval. Table4 summarizes clinician
ratings on explanation quality across three dimensions. Our
method consistently outperforms the baseline, indicating that
explanations were perceived as more relevant, faithful, and
useful.
Finally, on ethical considerations: our system provides
sources and reasoning, which is crucial in medical appli-
cations to avoid blindly trusting an AI. The multi-agent
approach allows tracing the steps (each query reﬁnement
and why). This aligns with calls for more interpretable AI
in healthcare.
4.10 Clinical walkthrough: cardiology use case
To further illustrate the practical utility and clinical relevance
of our multi-agent framework, we present a scenario-based
walkthrough centered on a cardiology case. This example
demonstrates the end-to-end retrieval process, highlighting
the iterative collaboration between the LLM planner and the
retrieval agent.
Case description (Initial Query) “55-year-old male with
intermittent chest pain for 3 days, ECG unremarkable, tro-
ponin 1.2 ng/mL, normal coronary angiography.”
Iteration 0: initial retrieval
• Query issued: Same as initial case description.
• Top retrieved documents (titles):
– “Angina with normal coronaries: a diagnostic chal-
lenge”.
– “MINOCA case report: non-obstructive myocardial
infarction”.
– “Microvascular angina mimicking ACS: a case series”.
• LLM analysis: “The query may be too broad. While rel-
evant documents are retrieved, the results include both
ischemic and non-ischemic causes. Clarifying the sus-
pected mechanism may help.”
• Reﬁned query suggested by LLM: “MINOCA case
in middle-aged male with elevated troponin and normal
coronaries.”
Iteration 1: reﬁned retrieval
• Query issued:“MINOCA case in middle-aged male with
elevated troponin and normal coronaries.”
• Top retrieved documents (titles):
– Myocardial Infarction with Non-Obstructive Coro-
nary Arteries (MINOCA): clinical insights”.
– “Diagnostic pathways for MINOCA in male patients”.
– “MINOCA in ACS: role of cardiac MRI”.
• LLM decision : “The results now focus on MINOCA
speciﬁcally. Satisfactory coverage achieved. Stopping
further reﬁnement.”
Final reranking and explanation Using the ﬁnal candidate
set, the LLM relevance evaluator assigned scores and pro-
vided rationales. Top-2 outputs:
• Doc 1: “Myocardial Infarction with Non-Obstructive
Coronary Arteries (MINOCA): clinical insights” →
LLM explanation: “Patient presentation and coronary
ﬁndings match closely with this study; useful insights
into diagnosis and follow-up.”
• Doc 2: : “MINOCA in ACS: role of cardiac MRI” →
LLM explanation: “This paper provides diagnostic imag-
ing strategies relevant to patients with elevated troponin
but no stenosis.”
This case demonstrates how our system transitions from a
broad symptom description to a clinically focused diagnosis-
driven retrieval. The iterative LLM reformulation signif-
icantly narrows the scope, and the ﬁnal LLM reranker
enhances result interpretability. Such explanations may help
clinicians trust and act upon retrieved evidence.
123

## Page 17

Journal of King Saud University Computer and Information Sciences (2025) 37 :267 Page 17 of 19 267
5 Conclusions
We introduced a novel LLM-augmented multi-agent retrieval
framework that advances the state-of-the-art in textual
case retrieval, using medical scenarios as a challenging
testbed. Unlike traditional retrieval systems or one-shot RAG
approaches, our method features a cooperative interaction
between an LLM agent and a search agent, enabling iterative
query understanding, reﬁnement, and evidence integration.
Through this design, we effectively address key limitations of
prior methods: the LLM agent’s reasoning mitigates semantic
gaps and query ambiguity, while the retrieval agent grounds
the process in factual data to prevent hallucinations.
Our experiments on multiple benchmarks (clinical notes,
biomedical literature, case reports) demonstrate substantial
performance improvements (4–8% absolute gains in ranking
metrics) over strong baselines including ﬁne-tuned neural
retrievers and off-the-shelf ChatGPT-based pipelines. These
gains are attributed to improved recall (ﬁnding relevant infor-
mation that lexical or static models miss) and improved
precision (the LLM’s ability to ﬁlter out false positives by
actually “reading” content). We showed that each component
– iterative querying, hybrid retrieval, and LLM reranking
– contributes to the overall result, and removing any piece
degrades performance. Notably, even in zero-shot settings
and with noisy inputs, our system proved robust, highlight-
ing the value of its design for real-world usage where queries
may be imperfect.
In terms of innovation, our work is ﬁrmly rooted in com-
puter science: we advanced methodologies in information
retrieval (with a new multi-step retrieval algorithm, formal-
ized with algorithms and mathematical models) and natural
language processing (with an intelligent use of LLMs beyond
end-to-end generation). The medical scenario served to val-
idate the method’s efﬁcacy in a high-stakes domain, but the
core techniques are general. For instance, the same approach
could be applied to legal case retrieval (an attorney ﬁnd-
ing past cases similar to a current one), or technical support
(retrieving similar bug reports or Q&A threads), or any
domain where queries are complex and data is extensive. The
fact that our model can incorporate domain knowledge via
retrieval rather than requiring huge domain-speciﬁc training
makes it attractive for low-data domains as well.
Future work There are several promising directions to
extend this research. First, we plan to explore multi-modal
case retrieval – e.g., retrieving not only text but also related
medical images (X-rays, MRIs) for a case. Our agent
framework could be expanded with a vision model agent,
orchestrated by the LLM (similar to how HuggingGPT links
multimodal model). Second, we aim to integrate a knowl-
edge graph of medical concepts as an additional tool for the
LLM agent. This could allow the agent to verify or query spe-
ciﬁc structured knowledge (for example, checking a drug’s
indication) during retrieval, further enhancing precision and
explainability. Third, scalability optimization will be impor-
tant: techniques like indexing LLM’s reasoning patterns or
caching query reﬁnements for similar queries could reduce
latency. We will also investigate training a smaller student
model to emulate the LLM agent’s behavior (model distilla-
tion), to deploy in resource-constrained settings. Moreover,
in order to make the framework viable in real-time clin-
ical workﬂows — where low latency and high reliability
are essential — we aim to investigate latency-bounded
retrieval strategies. This includes limiting the number of
agent iterations dynamically based on time budget, lever-
aging lightweight retrieval approximators, and exploring
distillation of the LLM agent into fast, edge-deployable
variants. Future work will evaluate the framework’s respon-
siveness under time-constrained settings such as emergency
triage or ICU decision support, where milliseconds matter.
We also consider asynchronous or parallel agent execution as
a way to meet throughput demands in high-volume hospital
systems.
Another avenue is employing reinforcement learning to
ﬁne-tune the LLM agent’s query strategy, using retrieval
performance as reward. This could automate the reﬁnement
policy beyond what we supervised it for, possibly discover-
ing even better strategies (like when to branch into multiple
related queries). Ensuring ethical and fair use is also crucial;
we need to monitor if the LLM agent exhibits any bias in how
it picks results (e.g., overly favoring cases it “remembers”
from training data which might not be the most relevant).
By keeping the human-in-the-loop (through explanations and
transparent sources), our system can facilitate auditing and
corrections.
In conclusion, our work demonstrates a viable path
towards next-generation retrieval systems that are not just sta-
tistical matching engines, but intelligent, interactive agents.
By leveraging LLMs for their reasoning and comprehen-
sion in concert with traditional IR for grounding, we achieve
superior performance and provide user-friendly outputs (with
rationales). We believe this paradigm can greatly enhance
retrieval tasks in many domains. As LLM technology evolves
(e.g., newer models with even better reasoning or domain
tuning), the efﬁcacy of such multi-agent systems is poised to
further improve, paving the way for retrieval systems that can
understand, explain, and adapt – key features for trustworthy
AI in critical applications like healthcare and beyond.
Author Contributions Lang Deng: Methodology, Writing–original
draft, Software, Formal analysis, Project administration, Resources.
Huanhuan Hu: Methodology, Writing–original draft, Software, Formal
analysis, Project administration, Resources. Kongjie Lu: Supervi-
sion, Methodology, Resources, Funding acquisition, Writing–review
& editing. Ping He: Supervision, Methodology, Resources, Funding
acquisition, Writing–review & editing.
123

## Page 18

267 Page 18 of 19 Journal of King Saud University Computer and Information Sciences (2025) 37 :267
Data Availability Statement The datasets used in this manuscript are
publicly available datasets. Detailed information about these datasets is
provided in Section Dataset of this manuscript.
Declarations
Competing interests The authors declare that they have no known
competing ﬁnancial interests or personal relationships that could have
appeared to inﬂuence the work reported in this paper.
Open Access This article is licensed under a Creative Commons
Attribution 4.0 International License, which permits use, sharing, adap-
tation, distribution and reproduction in any medium or format, as
long as you give appropriate credit to the original author(s) and the
source, provide a link to the Creative Commons licence, and indi-
cate if changes were made. The images or other third party material
in this article are included in the article’s Creative Commons licence,
unless indicated otherwise in a credit line to the material. If material
is not included in the article’s Creative Commons licence and your
intended use is not permitted by statutory regulation or exceeds the
permitted use, you will need to obtain permission directly from the copy-
right holder. To view a copy of this licence, visit http://creativecomm
ons.org/licenses/by/4.0/.
References
Achiam J, Adler S, Agarwal S, Ahmad L, Akkaya I, Aleman FL,
Almeida D, Altenschmidt J, Altman S, Anadkat S et al (2023)
Gpt-4 technical report. arXiv:2303.08774
Alsentzer E, Murphy JR, Boag W, Weng W-H, Jin D, Naumann T,
McDermott M (2019) Publicly available clinical bert embeddings.
arXiv:1904.03323
Brown T, Mann B, Ryder N, Subbiah M, Kaplan J, Dhariwal P , Nee-
lakantan A, Shyam P , Sastry G, Askell A et al (2020) Language
models are few-shot learners. NeurIPS
Bubeck S, Chandrasekaran V , Eldan R, Gehrke J, Horvitz E, Kamar E,
Lee P , Lee YT, Li Y , Lundberg S et al (2023) Sparks of artiﬁcial gen-
eral intelligence: early experiments with gpt-4. arXiv:2303.12712
Chen D, Fisch A, Weston J, Bordes A (2017) Reading wikipedia to
answer open-domain questions. arXiv:1704.00051
Dai Z, Zhao VY , Ma J, Luan Y , Ni J, Lu J, Bakalov A, Guu K, Hall KB,
Chang M-W (2022) Promptagator: few-shot dense retrieval from
8 examples. arXiv:2209.11755
Devlin J, Chang M-W, Lee K, Toutanova K (2019) Bert: pre-training
of deep bidirectional transformers for language understanding. In:
Proceedings of the 2019 conference of the North American chapter
of the association for computational linguistics: human language
technologies, volume 1 (long and short papers), pp 4171–4186
Formal T, Lassance C, Piwowarski B, Clinchant S (2021) Splade
v2: sparse lexical and expansion model for information retrieval.
arXiv:2109.10086
Gao L, Ma X, Lin J, Callan J (2023) Precise zero-shot dense retrieval
without relevance labels. In: Proceedings of the 61st annual meet-
ing of the association for computational linguistics (volume 1: long
papers), pp 1762–1777
G a oY ,X i o n gY ,G a oX ,J i aK ,P a nJ ,B iY ,D a iY ,S u nJ ,W a n gH ,W a n gH
(2023) Retrieval-augmented generation for large language models:
as u r v e y .arXiv:2312.10997,2
Gu Y , Tinn R, Cheng H, Lucas M, Usuyama N, Liu X, Naumann T,
Gao J, Poon H (2021) Domain-speciﬁc language model pretraining
for biomedical natural language processing. ACM Trans Comput
Healthcare (HEALTH) 3:1–23
Guo J, Fan Y , Ai Q, Croft WB (2016) A deep relevance matching model
for ad-hoc retrieval. In: Proceedings of the 25th ACM international
on conference on information and knowledge management, pp 55–
64
Hambarde KA, Proenca H (2023) Information retrieval: recent advances
and beyond. IEEE Access 11:76581–76604
He B, He X, Chen M, Xue X, Zhu Y , Ling Z (2025) Rise: reasoning
enhancement via iterative self-exploration in multi-hop question
answering. arXiv:2505.21940
Jeong M, Sohn J, Sung M, Kang J (2024) Improving medical reasoning
through retrieval and self-reﬂection with retrieval-augmented large
language models. Bioinformatics 40:i119–i129
Ji Z, Lee N, Frieske R, Y u T, Su D, Xu Y , Ishii E, Bang YJ, Madotto A,
Fung P (2023) Survey of hallucination in natural language gener-
ation. ACM Comput Surv 55:1–38
Johnson AE, Pollard TJ, Shen L, Lehman L-wH, Feng M, Ghassemi M,
Moody B, Szolovits P , Anthony Celi L, Mark RG (2016) Mimic-iii,
a freely accessible critical care database. Sci Data 3:1–9
Karpukhin V , Oguz B, Min S, Lewis PS, Wu L, Edunov S, Chen D,
Yih W-t (2020) Dense passage retrieval for open-domain question
answering. In: EMNLP (1), pp 6769–6781
Khattab O, Zaharia M (2020) Colbert: efﬁcient and effective passage
search via contextualized late interaction over bert. In: Proceedings
of the 43rd international ACM SIGIR conference on research and
development in information retrieval, pp 39–48
Kirk R, Dina D, V oorhees E, William RH (2016) Overview of the trec
2016 clinical decision support track. In: Proceedings of the 15th
text retrieval conference
Lazaridou A, Gribovskaya E, Stokowiec W, Grigorev N (2022) Internet-
augmented language models through few-shot prompting for open-
domain question answering. arXiv:2203.05115
Le Q, Mikolov T (2014) Distributed representations of sentences and
documents. In: International conference on machine learning.
PMLR, pp 1188–1196
Lee J, Y oon W, Kim S, Kim D, Kim S, So CH, Kang J (2020) Biobert: a
pre-trained biomedical language representation model for biomed-
ical text mining. Bioinformatics 36:1234–1240
Lewis P , Perez E, Piktus A, Petroni F, Karpukhin V , Goyal N, Küttler H,
Lewis M, Yih W-t, Rocktäschel T et al (2020) Retrieval-augmented
generation for knowledge-intensive nlp tasks. Adv Neural Inf Pro-
cess Syst 33:9459–9474
Mialon G, Dessì R, Lomeli M, Nalmpantis C, Pasunuru R, Raileanu R,
Rozière B, Schick T, Dwivedi-Y u J, Celikyilmaz A et al (2023)
Augmented language models: a survey. arXiv:2302.07842
Miotto R, Li L, Kidd BA, Dudley JT (2016) Deep patient: an unsu-
pervised representation to predict the future of patients from the
electronic health records. Sci Rep 6:26094
Mitra B, Craswell N et al (2018) An introduction to neural information
retrieval. Found Trends® Inf Retriev 13:1–126
Nachimovsky H, Tennenholtz M, Kurland O (2025) A multi-agent per-
spective on modern information retrieval. arXiv:2502.14796
Nogueira R, Cho K (2019) Passage re-ranking with bert.
arXiv:1901.04085
Nogueira R, Y ang W, Lin J, Cho K (2019) Document expansion by
query prediction. arXiv:1904.08375
Pal A, Umapathi LK, Sankarasubbu M (2023) Med-halt: med-
ical domain hallucination test for large language models.
arXiv:2307.15343
Qu Y , Ding Y , Liu J, Liu K, Ren R, Zhao WX, Dong D, Wu H, Wang H
(2020) Rocketqa: an optimized training approach to dense passage
retrieval for open-domain question answering. arXiv:2010.08191
Reimers N, Gurevych I (2019) Sentence-bert: sentence embeddings
using siamese bert-networks. arXiv:1908.10084
Roberts K, Alam T, Bedrick S, Demner-Fushman D, Lo K, Soboroff
I, V oorhees E, Wang LL, Hersh WR (2020) Trec-covid: rationale
123

## Page 19

Journal of King Saud University Computer and Information Sciences (2025) 37 :267 Page 19 of 19 267
and structure of an information retrieval shared task for covid-19.
J Am Med Inform Assoc 27:1431–1436
Robertson S, Zaragoza H et al (2009) The probabilistic relevance frame-
work: Bm25 and beyond. Found Trends® Inf Retriev 3:333–389
Santhanam K, Khattab O, Saad-Falcon J, Potts C, Zaharia M (2021)
Colbertv2: effective and efﬁcient retrieval via lightweight late
interaction. arXiv:2112.01488
Schick T, Dwivedi-Y u J, Dessì R, Raileanu R, Lomeli M, Hambro E,
Zettlemoyer L, Cancedda N, Scialom T (2023) Toolformer: lan-
guage models can teach themselves to use tools. Adv Neural Inf
Process Syst 36:68539–68551
Shen Y , Song K, Tan X, Li D, Lu W, Zhuang Y (2023) Hugginggpt:
solving ai tasks with chatgpt and its friends in hugging face. Adv
Neural Inf Process Syst 36:38154–38180
Singhal K, Azizi S, Tu T, Mahdavi SS, Wei J, Chung HW, Scales N,
Tanwani A, Cole-Lewis H, Pfohl S et al (2023) Large language
models encode clinical knowledge. Nature 620:172–180
Thakur N, Reimers N, Rücklé A, Srivastava A, Gurevych I (2021) BEIR:
a heterogeneous benchmark for zero-shot evaluation of informa-
tion retrieval models
Touvron H, Martin L, Stone K, Albert P , Almahairi A, Babaei Y , Bash-
lykov N, Batra S, Bhargava P , Bhosale S et al (2023) Llama 2:
open foundation and ﬁne-tuned chat models. arXiv:2307.09288
Tsatsaronis G, Balikas G, Malakasiotis P , Partalas I, Zschunke M, Alvers
MR, Weissenborn D, Krithara A, Petridis S, Polychronopoulos D et
al (2015) An overview of the bioasq large-scale biomedical seman-
tic indexing and question answering competition. BMC Bioinform
16:138
Wang C, Ong J, Wang C, Ong H, Cheng R, Ong D (2024) Potential for
gpt technology to optimize future clinical decision-making using
retrieval-augmented generation. Ann Biomed Eng 52:1115–1118
Wei J, Wang X, Schuurmans D, Bosma M, Xia F, Chi E, Le QV , Zhou D
et al (2022) Chain-of-thought prompting elicits reasoning in large
language models. Adv Neural Inf Process Syst 35:24824–24837
Xiong G, Jin Q, Lu Z, Zhang A (2024) Benchmarking retrieval-
augmented generation for medicine. In: Findings of the association
for computational linguistics ACL 2024, pp 6233–6251
Xiong L, Xiong C, Li Y , Tang K-F, Liu J, Bennett P , Ahmed J, Over-
wijk A (2020) Approximate nearest neighbor negative contrastive
learning for dense text retrieval. arXiv:2007.00808
Y ao S, Zhao J, Y u D, Du N, Shafran I, Narasimhan K, Cao Y (2023)
React: synergizing reasoning and acting in language models. In:
International Conference on Learning Representations (ICLR)
Zakka C, Shad R, Chaurasia A, Dalal AR, Kim JL, Moor M, Fong
R, Phillips C, Alexander K, Ashley E et al (2024) Almanac—
retrieval-augmented language models for clinical medicine. Nejm
ai 1:AIoa2300068
Zhao P , Zhang H, Y u Q, Wang Z, Geng Y , Fu F, Y ang L, Zhang W, Jiang
J, Cui B (2024) Retrieval-augmented generation for ai-generated
content: a survey. arXiv:2402.19473
Zhao Z, Jin Q, Chen F, Peng T, Y u S (2023) A large-scale dataset
of patient summaries for retrieval-based clinical decision support
systems. Sci Data 10:909
Publisher’s Note Springer Nature remains neutral with regard to juris-
dictional claims in published maps and institutional afﬁliations.
123
